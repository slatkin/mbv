id: no-wide-hero-split-outside-arrangement
valid:
  - |
    fn render(a: Rect) {
        let _ = wide_hero_presentation(a, None);
        let _ = wide_library_panes(a, 2, 1, None);
    }
  - |
    fn render(a: Rect) {
        let _ = wide_hero_hero_pane(f, a, LeftPaneFocus::ReadOnly, None);
    }
invalid:
  - |
    fn render(a: Rect) {
        let _ = wide_hero_split(a, None);
    }
  - |
    fn render(a: Rect) {
        let _ = wide_hero::wide_hero_split(a, None);
    }
