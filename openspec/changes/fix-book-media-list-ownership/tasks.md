# Fix Book Media-List Ownership Tasks

## 1. Characterize Book ownership and presentation

- [x] 1.1 Add or update focused Book component/media-list tests proving active-control-owned movement and selection for ordinary moves, bucket edges, and bucket cycling; stable-target ordinary refresh with local clamp; retained book-hit resolution and invalidation with blank no-ops; retained-claim-gated wheel behavior; bidirectional `ViewportAnchor` handoff preserving target plus selected-row offset; and `ActivateChapter(Option<usize>)` resolved carry; rewrite tests that assert `book_rows`, parent geometry, `browser_offset`, or painted-offset mechanisms, and verify the targeted component/media-list tests pass.
- [x] 1.2 Add and register `src/app/tests_tick_integration_book.rs`, plus an `abs_book_component_mut` tick-harness accessor mirroring Podcast, with Wide AND Normal/Narrow coverage through `Application::tick()`, `draw_frame`, `sync_mounted_surfaces`, and the shell sync pass proving navigation paints the same selected book, clicks resolve painted books while blank clicks are full no-ops with discriminating `raw_messages` assertions, wheel input is claimed only over the active control with both on-control and off-control legs, a breakpoint round-trip preserves target plus selected-row offset, and placeholder claims are invalidated at both breakpoints; verify the targeted shell-tick tests pass.
- [x] 1.3 Extend Book render-buffer tests with evidence that narrow selected-book detail includes chapter rows within the admitted replacement, hero metadata shows the required `%`/`Finished` state, surname pills remain in the shared pill row, and loading covers reserve the shared placeholder slot using `SERIES_IMAGE_COLS`/`SERIES_IMAGE_ROWS`; verify the targeted render tests pass at representative Wide and Normal/Narrow dimensions.
- [x] 1.4 Record and test the umbrella row-8.2 exclusion: `canonical-media-lists` says chapter rows remain provider-owned seek targets and provider workspaces remain parent-owned, while `audiobookshelf-book-browsing` requires chapter detail in the narrow selected-row replacement; verify tests preserve `chapter_selection`, `chapter_list`, `move_chapter`, `ChapterFocus`, chapter-row identity, and absolute seek mechanics while adding only the required narrow projection.

## 2. Transfer Book list and presentation authority

- [x] 2.1 Refactor `AudiobookshelfBookComponent` to add persistent `wide_book_list: WideMediaList<String>` beside the existing persistent narrow control; implement a `select_book()`-style path so movement, bucket edge/cycle selection, bucket-scoped clamp, click, and wheel move only the active control and return the resolved Book request; transfer responsive state control-to-control through one `ViewportAnchor`; delete `browser_offset`, `painted_row_offset`, `state.select(idx)` handoff reseeding, and per-frame Wide-control construction; keep content-struct cursor/select effect-path-only and verify focused tests cover control-owned selection, refresh, and handoff.
- [x] 2.2 Refactor the Book interaction and shell boundary so book clicks use the active control’s `claims_current_point`/`resolve_current_point` through a `book_index_at()`-style helper, blank space is unclaimed, wheel moves only a claiming active control and returns the resolved book request, and `ActivateChapter(Option<usize>)` carries component-resolved chapter selection without a shell component downcast; preserve exhaustive dispatch, event-scoped content projection, detail-fetch/persistence effects, chapter workspace mechanics, `ChapterFocus` no-op storage, and absolute chapter-seek authority, and verify retained-hit, wheel, and intent-carry tests pass.
- [x] 2.3 Refactor the Book render seam to remove `book_rows`, `push_book_rows`, render-time unconditional `select_target`/`set_scroll` reseeding, and book-position paint writeback; configure and view exactly one persistent active book control, retain only chapter-workspace and shell-owned chrome/image/overlay geometry, and call `invalidate_paint()` on both book controls for empty/loading paths in Wide and Normal/Narrow; verify placeholder invalidation, retained geometry, and no-underpaint tests pass.
- [x] 2.4 Converge Book presentation through existing shared helpers: render provider-owned chapter rows inside the narrow selected-book replacement with a bounded shared-style content-budget helper, adopt `SERIES_IMAGE_COLS`/`SERIES_IMAGE_ROWS` and the accepted loading-placeholder flag pattern, and leave conformant Wide composition, surname-pill data/chrome, and hero `%` metadata unchanged except for verification; verify render-buffer evidence passes without forking the named shared controls, painters, arrangements, detail-shell, hero-content, duration, palette, or tick-harness helpers.

## 3. Verify the bounded repair

- [x] 3.1 Run the relevant Book component, media-list, render-buffer, shell, and shell-tick tests at Wide and Normal/Narrow breakpoints; verify retained hits and placeholder invalidation, active-only navigation/wheel claims, target-plus-offset handoff, narrow chapter detail, mount through `sync_mounted_surfaces`, and base-frame reservation without underpaint. With cached chapters, verify exactly one **book rail** painter while allowing the legitimate chapter workspace’s second `render_wide_media_list` paint rather than asserting a global count of one.
- [x] 3.2 Run `cargo fmt --all -- --check`, `cargo check -p mbv`, relevant `cargo nextest run -p mbv`, `cargo clippy --workspace --all-targets`, and `ast-grep scan`; record any unrelated failure without widening this change. Do not run or add the pre-push-only file-size gate to this bounded acceptance list.
