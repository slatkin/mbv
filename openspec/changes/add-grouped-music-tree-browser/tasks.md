## 1. Dependency and artist identity

Decision trace: D2, D7, D8, D9.

- [x] 1.1 Add the exactly locked `tui-treelistview` 0.2.2 dependency without its optional keymap and create the destination-specific adapter boundary outside `music_content.rs`. Before building the tree owner, render one adapter frame at the existing Wide and smallest supported non-Wide Library-panel fixtures proving the tree's own row treatment, hierarchy glyphs, zebra rhythm, scrollbar, focused marquee, clipping, latest-render hit testing, and aggregate marks through only the crate's supported model/state/renderer/style seams. Verify with focused buffer `cargo nextest run -p mbv` cases plus `cargo check -p mbv`. This was the hard stop that would have rejected the dependency before tasks 1.2–6.5. Verified: the frame painted and hit-tested through supported seams; the crate exposes no focus gate for its scrollbar and paints only inside the rectangle it is given.
- [x] 1.2 Add `ArtistItems` to the relevant Music album/item `Fields` requests, extend Emby item parsing to retain name/ID pairs without changing serialized queue compatibility, and resolve an ID only from a trimmed case-insensitive exact name match; verify request fields and parsing for present, missing, multiple-unmatched, and equal-name/different-ID payloads with focused mock-HTTP and parsing `cargo nextest run -p mbv-core` cases.
- [x] 1.3 Carry stable artist identity through `GroupedAlbumCatalog`, define deterministic fallback grouping keys for every absent or unmatched `ArtistItems` case, and protect equal-name separation plus fallback stability with pure grouping cases; verify with the smallest relevant `cargo nextest run -p mbv` filter.
- [x] 1.4 Add the hermetic `mbv-core` client operation for `ArtistIds=<ArtistItems id>&IncludeItemTypes=Audio&Recursive=true`, never accepting IDs sourced from an `/Artists` listing, and verify query construction, parsed tracks, and unsupported/error propagation with focused mock-HTTP `cargo nextest run -p mbv-core` cases.

## 2. Stable tree owner and Library panel composition

Decision trace: D1, D2, D3, D4.

- [x] 2.1 Implement the destination-local node arena and shallow artist-root/album-leaf model with monotonic non-reused IDs, settled ordering, atomic model revision, and node-to-domain translation; verify focused model cases for refresh retention, deletion fallback, equal names, and destination reset with `cargo nextest run -p mbv`.
- [x] 2.2 Implement one tree state owner for selected node, expansion, viewport, marks, and geometry reconciliation across settled refresh and responsive geometry changes; preserve the selected node's prior viewport row when bounds permit and otherwise apply only the minimum scroll needed to keep it visible; emit album-selection persistence only when the resolved selected album changes, and never overwrite album persistence when an artist root receives focus; verify selected-node, expansion, multi-selection, exact viewport continuity/clamping, and the artist-focus persistence guard with component-level `cargo nextest run -p mbv` cases.
- [x] 2.3 Integrate the tree as the Grouped Music browser implementation of the existing Library panel list slot while keeping album-track and artist-track Workspaces on `MediaList`; remove the parallel album carrier/flat projection and verify Wide, Narrow, and Mini mounted composition has one browser owner and one painter with targeted `cargo nextest run -p mbv` integration cases.
- [x] 2.4 Map visible-node movement, Home/End, viewport paging, parent/child movement, and Enter expansion/album activation through `MusicContent` without enabling the crate keymap or changing central precedence; verify focused component behavior and routing-matrix rows with `cargo nextest run -p mbv`.

## 3. Tree rendering and pointer geometry

Decision trace: D4, D8, D9.

- [x] 3.1 Implement artist and album label/column rendering through the crate's supported renderer and column interfaces, following this change's pinned year-gutter contract directly: paint an album year once in the same right-aligned fixed six-column `STATUS_AVAILABLE` gutter, reserve no gutter for artist roots or yearless leaves, and add no inline/second year column (if the concurrent `unify-row-metadata-gutter` change later settles a different contract, reconcile here at spec sync rather than blocking on its lifecycle); also apply semantic roles for indentation, expand/collapse glyphs, aggregate marks, and scrollbar. Source album semantics through `MediaSemanticState::from_emby` and keep artist roots ordinary so raw played/resume fields cannot decorate Music tree rows. Verify the existing Wide and smallest supported non-Wide fixtures retain a hierarchy glyph and at least one title cell without overrunning the browser rectangle, plus exact gutter position/colour/reservation cases, with focused buffer `cargo nextest run -p mbv` cases.
- [x] 3.2 Implement the tree's own focused-node treatment, focus/unfocus distinction, group-relative zebra rhythm, multi-selected row treatment, truncation, and focused-title marquee through the crate's supported style and renderer interfaces; the tree's selected-row extent, zebra rhythm, and scrollbar focus behavior are tree-specific and need not match the canonical flat lists. Verify painter-owned buffer cases and title-clock reset behavior without snapshots or sleeps using `cargo nextest run -p mbv`.
- [x] 3.3 Retain and invalidate only latest-completed-render tree geometry, translate click/double-click/wheel/right-click gestures inside `MusicContent`, and remove any album-list geometry compatibility path; verify stale-frame rejection and current-frame artist/album hit resolution through component and mounted mouse cases with `cargo nextest run -p mbv`.

## 4. Artist actions and multi-selection

Decision trace: D6, D9.

- [x] 4.1 Resolve focused artist play, enqueue, shuffle, and context intents to ordered album targets, independent of expansion and restricted to matching leaves while filtered; verify collapsed, expanded, filtered, and equal-name artist cases emit album identities only with `cargo nextest run -p mbv`.
- [x] 4.2 Implement modified-click multi-selection so Ctrl+Click toggles album leaves and artist toggles affect visible descendant albums and roots derive tri-state marks; verify ordered membership, Partial/Marked state, filtering, and Queue-selection isolation with focused `cargo nextest run -p mbv` cases.
- [x] 4.3 Connect tree selection summaries and context-origin identity to the existing status and bulk-action paths, preserving capability intersection and clear-only-origin behavior; verify one mounted status/action integration case with `cargo nextest run -p mbv`.

## 5. In-place fuzzy filtering

Decision trace: D5, D9.

- [x] 5.1 Add the Grouped Music filter session using `SkimMatcherV2`, the existing 300 ms duration, composite artist/title/year text, ancestor retention, forced filter expansion, and settled-order projection; verify debounce by injected instants and cover match, no-match, empty-query, and stable-order cases with `cargo nextest run -p mbv`.
- [x] 5.2 Keep `MusicContent.inline_search` and its `InlineSearchHost` implementation as the sole query editor, debounce clock, open/close state, and search-bar projection; route each debounced Grouped Music query into the tree filter session and remove Grouped Music's `MusicContent::panel_content` `ListSlot::Search` swap so the tree owner rides `ListSlot::Media` through its object-safe `PanelList` implementation (per D5's method mapping) beneath the existing bar, without constructing or querying an Inline Search result carrier or dispatching a full-library fetch. Mask stored marks outside the filtered projection from counts, aggregate state, painting, and actions without deleting them, then restore the pre-filter node, persistent expansion, and surviving marks on dismissal. Verify one query/input owner, key precedence, no-fetch/no-flat-carrier behavior, hidden-mark masking/restoration, the filtered-album single-Enter dismiss→focus→non-Wide overlay cascade, and a responsive transition where the same corpus, query, selected node, forced expansion, and owner survive while the viewport clamps, with component and mounted `cargo nextest run -p mbv` cases.

## 6. Artist Hero and track Workspace

Decision trace: D1, D7, D9.

- [x] 6.1 Add typed artist-track request and completion variants with exhaustive shell dispatch arms, then build the shell-owned artist-detail cache and request identity on the verified task 1.4 client operation, keyed by Library destination, Service setup generation, `ArtistItems` ID, and settled revision; verify every variant's dispatch, cache reuse, explicit fallback/no-op handling, and stale completion rejection with hermetic `cargo nextest run -p mbv` cases.
- [x] 6.2 Add typed artist-artwork request and completion variants with exhaustive shell dispatch arms, fetch through the existing image/cache boundary, and project immediate artist summary facts from in-scope albums; verify every variant's dispatch, stable-ID artwork requests, explicit fallback no-artwork behavior, filtered album count, and year span with focused `cargo nextest run -p mbv` cases.
- [x] 6.3 Project cached artist tracks only for in-scope album IDs into a canonical `MediaList` Workspace grouped by settled album order and sorted by disc/track order, falling back to aggregation of existing per-album track fetches when no artist ID exists; verify duplicate titles, multi-disc order, filter re-projection without refetch, and fallback grouping with `cargo nextest run -p mbv`.
- [x] 6.4 Extend shared Hero/Workspace content so artist focus and album focus switch atomically in Wide and Library Hero overlay presentations, remove the Grouped Music album-only narrow inline-row Hero path, preserve existing album track behavior, map Right on an already expanded artist root to artist Workspace focus, and prevent stale artist/album tracks painting under a new title; verify Right first expands a collapsed root and only a later Right enters its Workspace, plus a non-Wide filtered album Enter that dismisses the filter, focuses the album, and opens its Workspace overlay in the same event, with focused component and shell tick `cargo nextest run -p mbv` cases.
- [x] 6.5 Re-key the existing neighbour album-artwork prefetch window to album leaves in the latest completed painted tree projection (one behind, three ahead). Have the tree owner resolve and emit the ordered stable album targets in a typed shell request; the shell applies the existing idle gate and fetches those targets without receiving or re-resolving a tree cursor. Suppress the request while an artist root is focused or a tree filter is active, and suppress fetches while the idle gate is closed; verify Wide and non-Wide projection order, typed target payload, both component suppressions, and shell idle-gate behavior with focused component and mounted shell tick `cargo nextest run -p mbv` cases.
- [x] 6.6 (Added 2026-09-20 by user decision during pre-acceptance testing) Project album tracks as selectable tree nodes — artist root → album leaf → track items ordered by disc/track order — so the keyboard can scroll to and activate a specific track in the same motion; source tracks from the shell-owned artist-detail cache (and the album-tracks path that populates it) without refetching, keep Hero/Workspace as the full-album listening surface (deliberate double functionality, accepted), and verify tree presentation of track rows, activation routing to existing playback arms, and KeepInView/pagination interaction with mounted shell tick `cargo nextest run -p mbv` cases, including a tick case pinning that artist-root selection movement arms the next page through the dispatch wiring.

## 7. Contract cleanup and acceptance

Decision trace: D8, D9.

Note (2026-09-20): the 7.4 tweak round's tweak batch (uncommitted at reconcile time, user-owned as part of this change) landed: the shared word-local fuzzy acceptance rule (`src/app/fuzzy_match.rs`) for every library search, per-node Grouped Music filter matching (each level matches only its own text; matched albums no longer drag their track rows), the restored library browser zebra stripe on `Surface::LibraryColumn`, the fixed dark `HeroPane` sheet shared with the Library Hero overlay, and `LibraryColumn`/`PillRowGap` following the panel focus bit. The affected delta specs were reconciled to this behavior; row 7.5 syncs them.

Note (2026-09-20, doc lane): two further landed behaviors from the same batch — the help chord-column automeasure and the `LibraryColumn`/`PillRowGap` panel-focus-bit wiring — carry delta coverage in `specs/configurable-keybinds/`, `specs/library-panel/`, and `specs/ui-design-system/` (the last covering the closed meaning-free palette vocabulary and its `docs/palette.json` mirror, added 2026-09-20 per the same user approval) (user-approved as intended); row 7.5 syncs those too.

Note (2026-09-20, acceptance): row 7.4 accepted by the user as an explicit terminal/visual pass. The accepted slice is the tweak batch landed in `aa9532ee`, `171dc088`, `e0b2953f` plus the residual fixes in `c0c9a7d2`. The configured Emby Service version and the `ArtistItems`/`ArtistIds` round-trip record the row asks for were not captured in this session; either outcome is valid because the fallback path is mandatory and is covered by tasks 1.3/1.4 tests. Residual roughness closed with the acceptance: the always-false `search_bar_loading` state was removed, the debounce-to-filter wiring is now pinned by a hermetic shell tick test, and a cleared selection no longer projects `Some(0)`. Non-blocking nit left as-is: the new debounce test hand-rolls an existing fixture instead of reusing `make_music_group_app_with_second_album`.

Note (2026-09-20, sync): row 7.5 applied all 10 delta capabilities into `openspec/specs/` in `94921761` (canonical-media-lists, configurable-keybinds, grouped-music-tree-browser — new capability spec, inline-library-search, library-hero-overlay, library-panel, media-list-multi-select, music-library-hero, stable-music-library-grouping, ui-design-system); `openspec validate --all` = 80 passed / 0 failed.

- [x] 7.1 Update or remove superseded Grouped Music flat-list/search tests in this change, while retaining existing canonical Heading/page-navigation behavior for other destinations; verify the targeted `mbv` test families pass without editing or depending on the separate `group-aware-page-navigation` change.
- [x] 7.2 Add the precise focusable Music artist-root term to `CONTEXT.md` while preserving `Group heading` as the non-selectable canonical-list term, and verify proposal, design, specs, code names, and glossary use the distinction consistently by review.
- [x] 7.3 Run `cargo fmt`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo nextest run -p mbv-core`, `cargo nextest run -p mbv`, and a targeted `cargo llvm-cov` review of the new model/filter/artist-completion paths; fix regressions and record any proven pre-existing failures.
- [x] 7.4 Hand the completed slice to the user and leave this task unchecked until they perform terminal acceptance: record the configured Emby Service version and whether requested album payloads carry `ArtistItems` and `ArtistIds` returns that artist's Audio tracks (either outcome is valid because fallback behavior is required), then review the existing Wide and smallest supported non-Wide fixtures plus Mini and Library Hero overlay states, including long names, clipping, expansion, filtering, marks, scrollbar, artist/album switching, and focused/unfocused treatments; accept only on explicit user satisfaction, working out residual visual roughness against this change's tree-specific contract; only a defect the crate's supported interfaces cannot address removes the dependency and its tree-specific implementation, never a bespoke parallel renderer.
- [x] 7.5 After implementation and terminal acceptance, sync every applied delta in this change into `openspec/specs/` with the `openspec-sync-specs` workflow and verify `openspec validate --all` passes.
- [x] 7.6 After synced specs and all prior tasks are complete, archive `add-grouped-music-tree-browser` with the `openspec-archive-change` workflow and verify the archived change plus main specs remain valid.
