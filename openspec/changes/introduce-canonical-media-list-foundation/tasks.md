## 1. Contract and baseline

- [x] 1.1 Add `WideMediaList` and `InlineMediaBrowser` terminology to `CONTEXT.md` before implementation; use the exact names and distinguish Inline Search.
  - Also add `Emby podcast channel list` to `CONTEXT.md` alongside the control terms (`Emby homevideos feed view` is already defined); this term is coined by this slice.
- [x] 1.2 Record current TV Wide→Narrow→Wide cursor, selected target, scroll, and selected-row screen offset as the pre-replacement baseline.
- [x] 1.3 Confirm the PR #606 stacking rule; this slice is a distinct PR targeting `feat/migrate-tui-to-tuirealm`. Mouse is out of scope and not a dependency: `restore-mouse-support` (#638) lands after every canonical slice.

## 2. Shared foundation

- [x] 2.1 Define provider-neutral `MediaListRow<Target>` item/heading/spacer model and semantic states, with stable opaque targets and bounded `0..=100` active progress.
- [x] 2.2 Re-home `render_plain_rows` without changing accepted generic Emby/TV output; retain semantic theme ownership and leave later-slice callers untouched. Applicable foundation Wide Browser paths stop reaching it, and the letter-grouped painter beside it, in task 3.5a.
- [x] 2.3 Implement embedded plain `WideMediaList<Target>` with selectable indexing, movement, clamping, viewport/scrollbar, fixed-height one-column geometry, and internal row geometry for painting and scrolling.
- [x] 2.4 Implement embedded plain `InlineMediaBrowser<Target>` with selected-row replacement, fit admission, fallback, anchor, and matching paint geometry; share only private mechanics with Wide.
- [x] 2.5 Implement `ViewportAnchor { selected_target, selected_row_offset }`; preserve target/offset across breakpoint transitions and clamp receiving geometry without shell mirrors.
- [x] 2.6 Extend `WideMediaList` to absorb letter grouping through the existing `MediaListRow::Heading`/`Spacer` vocabulary: bucket derivation, heading/spacer emission, sorted display order, and letter-filter interaction, so an applicable Wide path needs no second grouped painter. Match the accepted `render_letter_grouped_rows` output for generic Emby/Movies; a difference requires a named contract correction.

## 3. Foundation destinations

- [x] 3.1 Split `src/app/components/browser.rs` ownership-preservingly before or with wiring; keep it at or below 800 lines.
- [x] 3.2 Split `src/app/components/tv_workspace.rs` ownership-preservingly before or with wiring; keep it at or below 800 lines.
- [x] 3.3 Compose persistent `WideMediaList` and `InlineMediaBrowser` controls in `BrowserComponent` for every applicable Wide and Narrow path covering hero-bearing generic Emby catalog browsing, Movies, the Emby homevideos feed view, the Emby podcast channel list, and narrow TV Series browsing. The active child owns live cursor, scroll, selection, visibility clamp, row/replacement geometry, metadata placement, gutters, and scrollbar. Leave non-hero two-column Emby catalogs on their existing policy.
- [x] 3.4 Compose `WideMediaList` for the Wide TV right rail; preserve TV workspace, hero, pills, image handoff, effects, and parent message translation.
- [x] 3.5a Retire the Wide bespoke painters from applicable paths. No applicable Wide path may reach `render_generic_movies_home_video_rows_with_ctx` (`src/app/render/components/list.rs`), which routes to `render_letter_grouped_rows` whenever `true_total() >= 50 || letter_filter.is_some()` and to `render_plain_rows` otherwise; both branches SHALL be unreachable from canonical Wide paths. Absence of a `render_plain_rows` call alone is not sufficient evidence. Non-hero two-column browsers keep both painters and their existing policy.
- [x] 3.5b Export row geometry and the selected-row anchor from the persistent controls — display-row-to-target mapping in painted flow space, per-row screen rects, and the selected row's rect — so consumers read the painter's own plan instead of rebuilding it. This replaces the post-paint `InlineReplacementPlan` rebuild at `src/app/render/components/list_narrow.rs:246`.
- [x] 3.5c Re-point list-local movement in `src/app/components/browser/navigation.rs` (`move_sorted_cursor`, `move_by_item_rows`, `jump_cursor`) off `layout.left_sorted_indices` / `layout.left_item_rows` and onto the active control's own selectable order and viewport, per task 3.8.
- [x] 3.5d Re-point context-menu anchoring in `src/app/shell_overlays_menus.rs` off `layout.main.selected_item_rect` and onto the control-exported selected-row anchor from 3.5b. This consumer is not mouse and is not owned by `restore-mouse-support` (#638).
- [x] 3.5e Remove the per-frame `InlineMediaBrowser` construction (`list_narrow.rs:148`) and the independent replacement-geometry rebuild. `layout.left_row_map` SHALL be filled from the control-exported geometry of 3.5b rather than a second plan, and is recorded as the only remaining legacy geometry write — named pre-#638 mouse compatibility owned by `restore-mouse-support` (#638), kept wired and untouched. Prove no second list painter runs at an applicable breakpoint.
- [x] 3.6 Verify this slice's wiring left parent-owned pills, workspaces, overlays, and central keyboard routing unchanged and added no mouse subscription, `MouseGestureState`, `HitRegions<Target>`, or parent-to-child point delegation; confirm the existing bespoke `*HitRegion` paths remain wired and untouched. `restore-mouse-support` (#638) lands after every canonical slice and owns all mouse work.
- [x] 3.7 Split the Browser content channel from the position channel so canonical Browser input carries no cursor/scroll by construction; ignoring mirror fields is not sufficient. Add a position-free `BrowserContent` (items, `total_count`, `library_total`, `letter_filter`, `loading`, search query/loading, `group_pills`) as the sole input of `BrowserComponent::set_content` (`src/app/components/browser/mod.rs:110`) and delete the leg that re-seeds `self.cursor`/`self.scroll` from the pushed projection. `LibraryListRenderCtx` keeps its fields for its other consumers and is reconstituted for the legacy painters only inside `BrowserComponent`, at a single private site, from the control's own cursor/scroll; no shell-supplied position may reach it. Re-seeding position becomes an explicit, separately gated push: `BrowserComponent::apply_position` is called from `push_emby_browser_content` (`src/app/shell_browser.rs`) only when the browse identity changed — nav-stack depth, level `parent_id`, `letter_filter`, `sort_by`/`sort_order`, `unplayed_only`, and the selected feed/home-video group — which also closes the per-frame `feed_home_video.{video_cursor,video_scroll}` channel flagged in 3.3 review. Within one identity (pagination, loading completion, ordinary refresh, and the component's own `BrowserCursorIndex` echo) no position crosses the boundary. Keep the navigation-event and teardown resting persistence (`persist_emby_browser_scroll` -> `persist_library_scroll`): per D3 that is the shell-owned resting write, not a per-frame mirror; verify no per-frame scroll write-back remains in `render_emby_browser_component`. Bounded file set: the new `BrowserContent` module, `src/app/components/browser/{mod.rs,paint.rs}`, `src/app/shell_browser.rs`, and the `BrowserComponent::set_content` test sites (`browser_component_tests.rs`, `src/app/render/tests_conformance_matrix.rs`, `src/app/render/tests_non_music.rs`). Do not touch `LibraryListRenderCtx` itself, `App::library_list_render_ctx`, or its TV, Music, `detail.rs`, and inline-search consumers; see design D7.
- [x] 3.8 Delegate list-local movement to the active persistent control, map its resolved stable target to the existing typed request, and perform one explicit `ViewportAnchor` handoff at navigation or Wide<->Narrow transitions. Depends on 3.7's position channel: `ViewportAnchor` (breakpoint/target handoff) and `apply_position` (identity-gated resting re-seed) stay two distinct explicit seams and are not merged. Pairs with 3.5c.

## 4. Implementation evidence and gates

- [x] 4.1 Add or update the smallest representative stateful tests proving (a) `BrowserContent` cannot express cursor or scroll, so an ordinary Browser content push cannot carry them; (b) repeated pushes at an unchanged browse identity — pagination, loading completion, refresh — preserve the control's own cursor and scroll after movement; (c) a changed browse identity re-seeds position through the explicit `apply_position` push, preserving drill-in, go-back parent restore, letter-filter reset, saved-position restore, and feed/home-video group switching; and (d) selected target plus selected-row offset survive both a same-destination hero Browser Wide->Narrow->Wide transition and the TV Wide `TvWorkspaceComponent` <-> Narrow `BrowserComponent` round trip. Improve existing tests instead of duplicating them.
- [x] 4.2 Add or update focused rendered characterization/composition evidence with representative metadata, grouping, active progress, focus, breakpoint, and image fixtures. The existing foundation-owned characterization that asserts against the legacy geometry — `src/app/render/tests_library_characterization.rs`, `src/app/tests_narrow_browse_migration.rs`, `src/app/render/test_helpers_fixtures.rs`, and `src/app/render/tests_non_music.rs` — SHALL be rewritten against the control-exported geometry of task 3.5b, preserving each assertion's meaning. Retaining `layout.left_item_rows` / `left_sorted_indices` assertions in order to avoid rewriting them is not acceptable evidence, because it keeps the duplicate geometry alive. `src/app/render/tests_music_groups.rs` belongs to the Music/Audiobookshelf slice and is untouched here.
- [ ] 4.3 Record source-level one-painter and one-position-channel evidence for every migrated destination: no applicable Wide path reaches `render_generic_movies_home_video_rows_with_ctx` (and therefore neither `render_letter_grouped_rows` nor `render_plain_rows`), no Narrow per-frame canonical-control construction, no duplicate replacement geometry, and exactly one `LibraryListRenderCtx` construction site inside `src/app/components/browser/`, fed by control-owned position. State the evidence as reachability from the canonical paths, not as the absence of one named symbol or field.
- [ ] 4.4 Run `rtk make check-code-file-lines` and verify all changed source files are ≤800 lines.
- [ ] 4.5 Run `rtk openspec validate introduce-canonical-media-list-foundation --strict`.
- [ ] 4.6 Run `rtk cargo fmt`, `rtk cargo check -p mbv`, `rtk cargo nextest run -p mbv` for the relevant component/characterization/render suites, `rtk cargo clippy --workspace --all-targets`, and `rtk ast-grep scan`; fix only failures caused by this slice.

## 5. Slice review and acceptance

- [ ] 5.1 Review the complete implementation and evidence, then verify no registry identity, second router, callback/provider framework, global hit map, duplicate row coordinate path, protocol/provider/daemon/persistence change, or bespoke exception was introduced.
- [ ] 5.2 Perform live Wide/Narrow acceptance covering selection, movement, focus, scrolling, images enabled/disabled, and TV rail/Inline replacement. Treat any visual defect as a bug, fix it, and rerun the affected tests and gates before acceptance.
- [ ] 5.3 Attach stateful, rendered, source-level one-painter, live, file-size, and verification evidence; keep this slice independently reviewable/reversible and do not mark umbrella tasks complete until the corrected foundation is accepted.
