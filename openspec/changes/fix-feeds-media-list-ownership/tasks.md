# Fix Feeds Media-List Ownership Tasks

## 1. Characterize Feeds ownership and presentation

- [ ] 1.1 Update focused Feeds component/media-list tests to prove only the active control moves, selects, and resets; ordinary refresh preserves/clamps stable targets; blank, heading, and spacer geometry is unclaimed; Inline detail geometry resolves the selected FeedEntry; and Wide↔Normal/Narrow handoff preserves target plus selected-row offset through one `ViewportAnchor`; verify the targeted `feeds_component_tests.rs` tests pass without `left_row_map`, `left_item_rows`, or painted-offset assertions.
- [ ] 1.2 Add `src/app/tests_tick_integration_feeds.rs` with Wide AND Normal/Narrow coverage through `Application::tick()`, `draw_frame`, and the shell sync pass proving navigation paints the same selected row, clicks resolve painted FeedEntry targets while blank/header clicks are no-ops, wheel input is retained-claim-gated and active-control-only, exactly one list painter paints each frame with no base-frame underpaint, and a breakpoint round-trip preserves target plus offset; verify the targeted shell-tick tests pass.
- [ ] 1.3 Extend `src/app/render/tests_feeds.rs` with presentation-convergence buffer evidence for the shared pill-bar watched filter, canonical resume-progress badge/Active row slots without losing duration, the intentional hero placeholder slot, and pill-label truncation aligned with the Emby reference; verify the targeted render tests pass at representative Wide and Normal/Narrow dimensions.

## 2. Transfer Feeds list and presentation authority

- [ ] 2.1 Refactor `FeedsComponent` so movement, target selection, reset, click, and wheel affect only the active persistent control; gate wheel input with `claims_current_point`; resolve clicks through `resolve_current_point` plus the `current_detail_rect`/selected-target hero-cover rule; retain one control-to-control `ViewportAnchor` handoff; and delete the parent `painted_offset`, `left_row_map`, and `left_item_rows` maintenance; verify focused tests cover active-only movement and retained geometry.
- [ ] 2.2 Refactor the Feeds render path so the parent sets geometry and paint policy, invokes `Component::view` exactly once on the active control, and reads back only `current_selected_row_rect`/`current_detail_rect`; remove Feeds' compatibility free-function painter calls, selectable-map rebuild, and offset return while preserving base-frame reservation, framing, parent-owned groups/filter/hero/images/effects, and typed intent translation; verify one-painter and no-underpaint tests pass at both presentations.
- [ ] 2.3 Converge Feeds presentation by replacing the hand-rolled watched selector with the shared `render_pill_bar` primitive, projecting resumable unplayed entries through canonical trailing/Active row slots, correcting the `feeds_model.rs` hero-image comment to justify the retained placeholder box, and aligning `MAX_LABEL` with the Emby pill truncation policy only if it differs; verify the presentation buffer tests pass without modifying shared helpers or adding persistence/request state.

## 3. Verify the bounded repair

- [ ] 3.1 Run the relevant Feeds component, media-list, render-buffer, and shell-tick tests at Wide and Normal/Narrow breakpoints; verify retained hits, target-plus-offset handoff, active-only wheel/navigation, the base frame reserving without underpainting, and exactly one list painter per frame.
- [ ] 3.2 Run `cargo fmt --all -- --check`, `cargo check -p mbv`, relevant `cargo nextest run -p mbv`, `cargo clippy --workspace --all-targets`, and `ast-grep scan`; record any unrelated failure without widening this change.
