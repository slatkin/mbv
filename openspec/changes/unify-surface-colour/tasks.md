## 1. The one behaviour kept from `focus-aware-column-surface`

- [x] 1.1 `FrameChromeGeometry` carries the right column's focus: add `right_focused` (`src/app/layout.rs`) and derive it in `src/app/render/arrangements/chrome.rs` as `right_visible && PanelFocus::Library`, false whenever the right column is not visible. Verify: geometry tests cover wide `LibraryOnly`, wide `Both` with library focus, wide `Both` with queue focus, `QueueOnly`, and the narrow widths — including that a stale `Library` focus with no visible column yields false.
- [x] 1.2 The right-column backdrop resolves from that bit: `render_legacy_backdrops`' right arm (`src/app/render/components/chrome.rs`) renders the focused column surface (`#3c4841`) while the library column holds focus and today's `#2d353b` (`SURFACE_BACKDROP`) otherwise, with the left/queue arm untouched. Verify: a buffer test asserts the gutter around a focused library panel at wide `LibraryOnly` and wide `Both`, and the resting value once the queue takes focus.
- [x] 1.3 The playback panel owns only its content rows: the panel must not paint the row directly above the pill bar in the wide layouts, so that row shows the library column surface from 1.2; `QueueOnly` keeps painting it with its `#2d353b` fill and its `On Now:` title (`src/app/render/components/chrome_player.rs`). Verify: a buffer test shows the row above the pill bar following the library column surface in both focus states while the panel's own rows keep exactly their current colours, and the `QueueOnly` row and title are unchanged.
- [x] 1.4 Run the gates for section 1: `cargo nextest run -p mbv`, `cargo check -p mbv`, `cargo clippy --workspace --all-targets`, `cargo fmt --all`, `ast-grep scan`. Verify: all pass with no new warnings.

## 2. The inventory

- [x] 2.1 Classify every production paint site: for each site that writes a surface colour, record `file:line`, the surface identity it is, its nesting level, its owner painter, the rect source, and the focus state it depends on. Cover every production hit of the surface roles and resolvers in `src/app` excluding `theme/`, the re-export modules and tests, and every site the six-mechanism audit names (direct role names, duplicate roles, `SURFACE_BACKDROP`'s six meanings, per-screen colour bits, shell-owned geometry). Verify: the table names a surface identity for every site and every site in the grep appears; the sites that change no colour are listed as such with a reason; and each proposed name is **structural, not screen-shaped** — the same structural position in a different screen or provider resolves to the same name (a pane's content box is one name whether it holds a series list, a track list or chapters; no `Tv…` / `Music…` / `Abs…` variant), with the surface's *payload* left to content. Names SHALL comply with `CONTEXT.md`'s reserved terms and avoid-lists: `Panel` stays Library/Queue (so a sidebar is not a panel; `SidebarBody` / `SidebarBand`), the overlay term is **popup** (not dialog), "Main content box" is the pane inset's name (not "hero content box"), and "separate detail block" is avoided. The set SHALL be marked `proposed sketch` in the artefact, the canonical declaration being the theme in row 4.1, pinned by the 5.2 conformance test. No two names may be indistinguishable (e.g. `QueuePanel` vs `QueuePanelChrome`, `SidebarPanel` vs `SidebarChrome`, `StatusBar` vs `StatusPill`).
- [x] 2.2 Reconcile the inventory against the audit in `proposal.md`: state which sites are reachable by a role edit, which are skipped by it, and which choose their own colour. Verify: the numbers in `proposal.md` are either confirmed from the current tree or corrected in place.

## 3. One focus state

- [x] 3.1 Add `FocusState` — the active column plus whether the right column is visible, and deliberately **no** sub-surface or cursor, because selection never changes a colour (D1) — and compute it once per frame in the shell; thread it into `FrameChromeGeometry` and the screens' render contexts in place of `queue_focused` / `right_focused`. Choose names that do not collide: `right_focused` already means the Wide-hero right *pane* bit in `tv_wide.rs` / `music_wide.rs` — a different surface with a different resting value — so the collapsed state names its column explicitly (`Column::Right`). Verify: a geometry/state test shows the value changing with `PanelFocus` and the visible mode (including that a stale `Library` focus with no visible right column reports no focused right column), no screen computes its own colour bit, and no name in the new state duplicates a per-screen sub-focus bit.
- [x] 3.2 Delete the per-screen colour bits (`episode_focused` / `track_active` / `chapter_focused` as colour inputs) wherever they exist, keeping the same values as behaviour inputs for selection gating, cursor ownership and hit geometry. Also fold in the two derivations the row 3.1 review found still outside the state: the shell's own projection in `shell_playback.rs` still compares `effective_panel_focus()` with `PanelFocus::Queue` to choose the now-playing panel's fill, and `FrameChromeGeometry` still carries a `right_visible` that duplicates `focus.right_visible`. Verify: each screen's panel colours depend only on the supplied `FocusState`, proven by a buffer test per screen that moves the inner selection and observes no fill change; and `rg -n "effective_panel_focus\(\)|effective_panel_mode\(\)" src/app -g '!**/*tests*'` returns no render-time colour decision.

- [x] 3.3 Make selected-row highlight gating follow the cursor's sub-panel on every wide hero screen. ABS podcasts' show rail is gated by the column focus rather than the inner bit (pre-existing), so both rails show a selected row while the episode pane holds the cursor — which contradicts the delta spec's "the selection moves inside a focused pane" scenario (the sub-panel that gained the cursor shows its selected row and the other does not). Row 3.2 left it alone because that row's rule was "highlight gating must not move"; this row is where it moves. Verify: a per-screen buffer test asserts the highlight is present only in the cursor's sub-panel, in both inner-cursor positions, for ABS podcasts and for any other screen whose rail paint policy receives the column focus instead of the inner bit.

- [x] 3.4 Close the highlight-coverage gap on the other two wide hero screens. Music's and ABS books' buffer tests prove the rail's selected-row highlight appears and disappears but never assert the *other* sub-panel's highlight, so the delta spec's "the sub-panel that gained the cursor shows its selected row and the other does not" is unproven on those two screens (TV and ABS podcasts prove both halves). ABS books' chapter highlight is observable only through the selection marker glyph, because that row's background equals the content box's background. Verify: each of the two screens' tests asserts, in both cursor positions, that the highlight is present in the cursor's sub-panel and absent in the other, through the observable the production code actually uses.

## 4. One surface table

- [x] 4.1 Add the closed `Surface` identity set and `surface_colors(surface, &FocusState) -> SurfaceColors`, one row per surface carrying its level and the role it resolves. Place the two identities the inventory deliberately left open rather than guessing: `QueueCardVisualizer` is listed at `recess` but its colour resolves `PanelFocus::Queue` like a content body, since the card is the queue column's now-playing content and not an inset inside a panel; and `SelectedRow` has a second appearance at the column/pane level — `ACCENT_ACTIVE` in the context menu (`context_menu.rs:38`) — which is either retired or declared as a named variant. Verify: a unit test asserts every surface resolves both focus states, that a level's surfaces share the level's role values while other levels do not, and that the declared set covers every identity in the task 2.1 inventory with none left unplaced.
- [ ] 4.2 Migrate one screen at a time onto the table (Home, Feeds, library rails, TV, Music, ABS books, ABS podcasts, the queue column and panel, the playback strip, the popup frames), each as its own commit, deleting that screen's role names and resolver calls. No screen may choose a surface's colour: the ten popup callers that pass `SURFACE_FOCUSED`, `tv_wide.rs`'s episode box, `music_wide.rs`'s variant choice, `home.rs`'s spacer and `card.rs`'s visualizer must each name their surface identity instead. One transitional seam is allowed and must not become permanent: the shared painters that receive only a collapsed focus bool call `surface_colors_for_column_focus(surface, own_column_focused)`, whose parameter is documented as this surface's own column focus and never a cursor bit; the unit that owns those component render contexts threads the table's `FocusState` (or the equivalent own-column fact) so the bool cannot be a per-screen cursor bit. Verify per screen: `cargo nextest run -p mbv` passes with no expected-value change, and the diff contains no `SURFACE_*` name or resolver call in that screen.
- [ ] 4.3 Retire the duplicate and multi-meaning roles the inventory proves are aliases, one at a time, each with the picture unchanged: `SURFACE_ACCENT_SOFT` (two roles, one panel level), `SURFACE_PLAYBACK` (`SURFACE_RESTING`), `SURFACE_STATUS_PILL` (`SURFACE_CHROME`), `SURFACE_ARTWORK_PLACEHOLDER` (`SURFACE_BACKDROP`), `BORDER_UNFOCUSED` used as a fill, and the surface role that aliases a text role (`SURFACE_FOCUSED` = `TEXT_ACCENT_MUTED`). Also delete the transitional `#[cfg(test)]` re-exports the migrations leave behind (`list_selected_row_bg`, `SURFACE_ACCENT_SOFT`, `SURFACE_ARTWORK_PLACEHOLDER` in `src/app/palette.rs` and `src/app/render/mod.rs`), which exist only because tests still name them. Verify: `cargo nextest` green with no expected-value edit per role, the audit's alias list empty at the end, and `rg` finds none of those names in production or in the re-export modules.
- [x] 4.4 Remove the raw colour literal in a production painter: `Color::Rgb(73, 81, 86)` in `src/app/render/components/chrome_tabs.rs:131` becomes the chrome-band role, since raw primitives are private to the theme. Verify: no `Color::Rgb` remains in `src/app/render/components/**` or `src/app/components/**` outside tests (grep), and the tab-number buffer test still paints the same colour through the role.
- [x] 4.5 Give every surface exactly one painter where the inventory found more than one owner: the library/rail panel body (a shared `wide_hero_browser_border` fills it and six screens fill the same rect first) and the TV episode box (filled once as a recess and then as a panel). Verify: for each, one painter fills it and the others stop; the buffer tests at TV, Music, ABS books/podcasts, Feeds and the wide library pass with no expected-value change.
- [x] 4.6 Make the pane's content boxes follow their pane's focus, which is the defect the user reported: TV's overview box paints a fixed `#2d353b` and stays dark while the episode selection box lights up beside it, and the inline-hero content box does the same. Both take the declared soft content-body variant while their pane holds focus and keep today's `#2d353b` while it rests (the always-`ReadOnly` panes of Home and Feeds keep `#2d353b` in both states, since they can never hold focus). Verify: buffer tests show TV's overview box and the inline-hero box matching the episode selection box's fill while the pane holds focus and returning to `#2d353b` when it does not, with the Feeds and Home boxes unchanged in both states.

## 5. Guardrails

- [ ] 5.1 Add the ast-grep rule that fails on surface role names and `resolve_surface_*` calls in production screens outside `src/app/render/theme/`, and wire it into `ast-grep scan`. Verify: `ast-grep scan` passes on the migrated tree and fails when a role name is reintroduced in a screen (prove it once, then revert the probe).
- [ ] 5.2 Add the conformance test enumerating every `Surface` × breakpoint × focus state, asserting the rendered rect's fill equals the table's value. Verify: the test covers every table row, fails if a row's painter is changed to another level's colour (prove it once, then revert), and its expectations are generated from the table rather than duplicated by hand.

## 6. Verify

- [ ] 6.1 Run the gates on the finished change: `cargo nextest run -p mbv`, `cargo check -p mbv`, `cargo clippy --workspace --all-targets`, `cargo fmt --all`, `ast-grep scan`. Verify: all pass with no new warnings, no test deleted or weakened, and no expected colour value changed except where a task states it should.
- [ ] 6.2 Post the migration report: the inventory's coverage, the aliases retired, the level edits that now reach every surface, and the roles left with more than one meaning. Verify: each claim is reproducible from the tree (a grep or a test name), and anything still aliased is recorded as an open item rather than left silent.
