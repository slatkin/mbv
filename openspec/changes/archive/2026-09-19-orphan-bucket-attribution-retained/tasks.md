# Tasks

## 1. Supersede stale D3 wording

- [x] 1.1 Restore unmatched orphan buckets under their original track
  `ParentId` key after attempted `Path` attribution.
- [x] 1.2 Restore the whole-level later-page coverage and inert-row regression
  tests from `fix-music-artist-resolution-batching`.
- [x] 1.3 Document why retaining an unknown key is required for page-bounded
  fills and empty-album-list warm-up.

## 2. Verification

- [x] 2.1 Validate this record with `skip_specs: true`.
- [x] 2.2 Archive the accepted supersession record.
