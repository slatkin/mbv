# Tasks: skip-empty-music-folders

## 1. Empty-folder filtering

- [x] 1.1 Parse `ChildCount` into `EmbyItem` as `child_count: Option<u32>` (serde default `None`; explicit `0` = known-empty) in crates/mbv-core (api_types.rs + the item parse behind `get_items_sorted_ranged`, which already requests the `ChildCount` field). Unit tests: explicit 0 parses as `Some(0)`, absent field stays `None`.
- [x] 1.2 In grouped music views only (capture the same gate `is_music_group_view` uses — music library whose configured levels start with `"group"` — before the worker thread), drop `is_folder && child_count == Some(0)` items from level listings in `spawn_browse` and `spawn_browse_page` (src/app/library_browse_actions.rs), and from `spawn_music_group_warmup`'s group-level listing (src/app/image_fetch.rs). Keep pagination exact under filtering: track server rows consumed on `BrowseLevel` (`fetched_rows`, equal to `items.len()` when nothing is filtered), use it for `maybe_fetch_next_page`'s `start_index` and add the exhaustion guard `fetched_rows >= total_count` (a fully filtered page must terminate, not refetch-loop); persist it on `LibraryPositionLevel` as `Option<usize>` (`None` = existing snapshot → `items.len()`). Verify with unit tests (mock JSON/client, no live servers): empty folder omitted at group and album levels; `None` child count kept; non-folder with `ChildCount: 0` kept; non-grouped libraries unaffected; warm-up spawns no fill for an empty group child; page 2 fetches at the correct server offset after a filtered page; no refetch loop when the final page is fully filtered; restored positions behave with and without the persisted field.
- [x] 1.3 Verify `cargo nextest run -p mbv`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo fmt --all -- --check`, and `openspec validate skip-empty-music-folders` all pass.
