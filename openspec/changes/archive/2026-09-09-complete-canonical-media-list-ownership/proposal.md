# Complete Canonical Media-List Ownership — Retired Campaign Record

## Status

This planning-only umbrella is retired. It records the accepted baseline and scope reconciliation that preceded the abandoned campaign; it is not an active implementation plan, proposal generator, or authorization to create follow-on changes.

## Recorded purpose

This record preserves the historical decisions made around issue #681:

- PR #684 (`f647136a`) established the accepted Queue and Grouped Music painting/geometry foundation.
- The visible-screen inventory and preserved workspace boundaries were reconciled in the umbrella before the campaign was retired.
- Non-grouped Music views were explicitly kept out of the Grouped Music scope and were not silently classified as complete.

The original follow-on sequence was withdrawn. No screen implementation, source change, main-spec sync, or new proposal is requested by this record.

## Historical scope

The accepted baseline rows are retained in `tasks.md` with their provenance. The former future rows for Grouped Music, Home, Feeds, related Emby screens, TV Series, Audiobookshelf Podcasts, Audiobookshelf Books, and final enforcement/documentation work are withdrawn rather than complete.

The mistaken planning-only `complete-grouped-music-list-ownership` artifacts were created in `5030adcd` and removed by revert `bd243f34`. They do not form an active follow-on.

## Future planning boundary

Future screen work must begin interactively when the user selects a concrete problem or visible screen. Exploration, source evidence, preserved-boundary decisions, and scope must be discussed and explicitly authorized before one bounded proposal is created. A later, separate apply request is required before implementation begins. This retired record does not initiate any of those steps.

## Impact

- No runtime behavior, source code, API, dependency, persisted-data, or main-spec change is defined here.
- The out-of-scope decision for non-grouped Music is historical campaign bookkeeping only; it does not assert that those views are correct, complete, or committed to a future backlog item.
- Issue #681 is superseded as an active campaign tracker and should point to this retired record for history.
