## 1. Wire normalisation (mbv-core)

- [x] 1.1 Add `description` to `AudiobookshelfDownloadedEpisode` and normalise `published_at` to unix seconds at the wire boundary: accept Audiobookshelf's epoch-millisecond number (its actual shape for downloaded episodes), epoch-second numbers and strings, and ISO-8601 or RFC 2822 text; a missing or unreadable value is `None` and groups as `Unknown date`. Verify with unit tests covering each accepted form, the missing case, and the unreadable case (`cargo nextest run -p mbv-core`).
- [x] 1.2 Keep exactly the two existing episode sources — the bounded page fetch of the library's items (shows only) and the per-show expanded-item fetch — and add fixtures for their real shapes: a show page, an expanded show carrying episodes and their descriptions, a show with no downloaded episodes, and a malformed response. Verify `audiobookshelf_catalog_tests.rs` decodes each fixture (`cargo nextest run -p mbv-core`).

## 2. Browse state restructure

- [x] 2.1 Restructure `AudiobookshelfBrowseState` for the flat episode browser: keep the paged show list (it feeds the pill bar) and replace the surname/title buckets and the selected-show episode scalar with a per-show episode cache filled by the fan-out. Keep `(library_item_id, episode_id)` identity, the `needs_page` discipline, append-at-most-once, and generation reconciliation. Verify by rewriting the `types_audiobookshelf_browse.rs` unit tests (progressive append per show, dedupe, selection survival, refresh-removes-selected) and deleting the bucket tests (`cargo nextest run -p mbv`).
- [x] 2.2 Implement display-row grouping: reuse `feeds_model.rs` `FeedAgeGroup`/`feed_age_group` (one visibility change) behind a `PodcastDisplayRow` heading/spacer builder whose empty groups are omitted and whose episode indices are unchanged. Sort the flat slice newest-first before grouping. Verify with a unit test mirroring `display_rows_insert_non_selectable_groups_without_changing_indices` on podcast fixtures, including undated episodes → `Unknown date` (`cargo nextest run -p mbv`).
- [x] 2.3 Re-point the show-selection callers: `selected_audiobookshelf_queue_item_target` resolves the queue item's `show_title`/`author`/`cover_path` from the episode's own `library_item_id` instead of the current selection; the saved-position path no longer treats a show id as the tab's selection; the detail/shows completion arms feed the new cache. Verify with unit tests for each (`cargo nextest run -p mbv`).

## 3. PodcastContent owner rewrite

- [x] 3.1 Rewrite `PodcastContent` to the Feeds-owner shape: one episode `MediaListCarrier<PodcastEpisodeTarget>`, one pill `SelectorRow` (`All` / `Unplayed` / `Played` then per-show pills truncated like feed-group labels), a hero without a Workspace, and no selection modal or inline detail. Verify `content()` projection tests: pill order and truncation, split rows (podcast name, episode title, duration, played/in-progress state), grouped rows, and the empty and loading list slots (`cargo nextest run -p mbv`).
- [x] 3.2 Store the pill selection by value (`All`/`Unplayed`/`Played`/show `library_item_id`), derive the painted active index each frame, and implement `[`/`]` as one uniform walk of the bar. Keep the remembered pill across tab switches and reset it to `All` on construction. Verify with unit tests for the library-ui selector scenarios, including keyboard navigation and the remembered pill (`cargo nextest run -p mbv`).
- [ ] 3.3 Implement lazy, scoped loading: the active pill determines the required shows, requests are bounded in flight, each show is fetched at most once per session, and results append as they arrive. Refresh the active pill's shows on tab activation and on the refresh key. Verify with owner unit tests plus a real `Application::tick()` integration test through the shell sync pass (`cargo nextest run -p mbv`).
- [ ] 3.4 Re-aim activation: row Enter/double-click → `PodcastEpisodeIntent::OpenOrPlay(target)`, Ctrl+A → `Enqueue`, context menu unchanged, and no overlay or inline-detail path for podcast episodes. Verify with owner unit tests plus the tick-integration test (`cargo nextest run -p mbv`).
- [ ] 3.5 Hero facts through the single episode producer: episode title, podcast name, duration and publish date, with the episode description as the overview and the parent show's cover in the Square slot — no credits block, no author row, no Workspace. Delete `hero_content_abs_show`. Verify hero projection unit tests (facts, missing metadata collapse, images-disabled budgeting, selection-change refresh) (`cargo nextest run -p mbv`).

## 4. Shell and rendering integration

- [ ] 4.1 Update shell sync/push paths and image projection for the flat list: episode-arrival pushes, the pill remembered across destination switches, parent-show cover keyed by the episode's `library_item_id`, and stale-result rejection after Service replacement. Verify with tick-integration tests through the shell sync pass (`cargo nextest run -p mbv`).
- [ ] 4.2 Keyboard: `[`/`]` remain owner-local pill navigation (the Feeds precedent) and add no shell routing arm; remove the now-dead podcast episode-filter transition variants and the no-op arm, leaving every boundary-crossing request variant with an exhaustive arm or a documented no-op. This change's `mouse-input` delta already corrects the wheel verification record's podcast row (episode-row geometry, pill and episode selection); `docs/architecture/interactive-surface-ledger.md` names owners generically and needs no edit. Verify with owner unit tests and, if a shell-level row is genuinely needed, `src/app/tests_routing_matrix*.rs` (`cargo nextest run -p mbv`).
- [ ] 4.3 Paint coverage: prove the podcast panel paints its complete placement (pill row, grouped list, Wide hero without a Workspace, ordinary non-Wide rows) with one painter per surface in both geometries; update the buffer and pinning tests that referenced show rows or surname buckets. Verify with `cargo nextest run -p mbv` (`tests_tick_integration_podcast.rs`, panel paint tests).

## 5. Cleanup and gates

- [ ] 5.1 Delete dead podcast machinery: surname/title bucket use in this tab, the show hero workspace, the selection modal path, `hero_content_abs_show`, and now-unfed shell message arms (every boundary-crossing request variant gets an exhaustive arm or a documented no-op). Inline Search is a non-goal: this tab gains no `InlineSearchHost`, so no search work is in scope. Verify `cargo clippy --workspace --all-targets -- -D warnings` and no `#[cfg_attr(not(test), allow(dead_code))]` gates left on deleted items.
- [ ] 5.2 Full gates: `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo nextest run -p mbv` and `-p mbv-core`, and `openspec validate --all` green; then sync deltas and archive the change.
