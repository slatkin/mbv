# Tasks

## 1. Baselines and required file split

- [x] 1.1 Create `openspec/changes/extract-shared-list-components/baseline.md` and record the pre-change production line total from `wc -l src/app/components/media_list/{mod,wide,carrier,anchor,selection,grouping}.rs src/app/components/music_tree{,_model,_view,_selection,_label}.rs`. Run `cargo llvm-cov -p mbv --json --summary-only --output-path target/extract-shared-list-components-before.json`; record the summed covered-line and count-line values for that exact production file list, with the command and per-file values. Verify: the markdown contains the command, file list, line total, and coverage numerator/denominator needed for the after comparison.
- [x] 1.2 Split `src/app/components/media_list/mod.rs` into cohesive files under the 800-line cap, moving code only with no signature or behavior changes. Do not split an already-under-cap tree file unless work in this change would otherwise push it over 800 lines. Verify: `cargo check -p mbv`, `cargo nextest run -p mbv`, and `cargo fmt --all -- --check` are clean; review the diff as move-only.
- [x] 1.3 Run `cargo clippy --workspace --all-targets -- -D warnings` and `cargo fmt --all -- --check`. Verify: both are clean before behavior work begins.

## 2. The shared seam

- [x] 2.1 Define `RowFlow<Target>` in a new `src/app/components/list/` module: ordered rows, selectable-with-target versus structural-without, `len`, `row_at`, and `position_of(&Target)`. It has no dependency on `tui-treelistview` or `media_list` types. Verify: `cargo nextest run -p mbv 'test(/app::components::list/)'` covers structural rows occupying positions without being addressable.
- [x] 2.2 Add `Cursored` with primitive selected-target read/write hooks and shared default methods for move, first, last, index, and `select_target`, skipping structural rows and clamping at both ends. Shape adapters may implement only the primitive hooks, not movement arithmetic. Verify: `cargo nextest run -p mbv 'test(/app::components::list/)'` covers structural skipping, clamp-no-wrap, and absent targets.
- [x] 2.3 Add `Viewported` with primitive offset read/write hooks, shared clamp and keep-selection-visible defaults, and explicit fixed-selectable-distance versus visible-viewport paging policies. Verify: `cargo nextest run -p mbv 'test(/app::components::list/)'` covers offset preservation, minimum scroll, geometry clamping, and both paging policies.
- [x] 2.4 Add the shared retained-paint state carrier and `PaintRetained` operations for begin, finish, invalidation, `claims_point`, `resolve_point -> Option<&Target>`, and `selected_row_rect`. Shape painters supply completed target-bearing geometry but no caller can access a row map. Verify: `cargo nextest run -p mbv 'test(/app::components::list/)'` covers invalid-state claims and stable-target resolution.
- [x] 2.5 Add the shared ordered-mark state carrier and `MarkSelection`: add/remove retains addition order, while a shared row-flow projection returns selected targets in current display order for actions. Aggregation and flat-only Visual range/anchor state remain excluded. Verify: `cargo nextest run -p mbv 'test(/app::components::list/)'` covers interleaved addition/removal and click order differing from emitted action order.
- [x] 2.6 Add `Expandable` as the tree-only adapter contract for stable-target expansion, parent/child movement, and aggregate mark state; keep expansion storage in `TreeListViewState` and shared post-change cursor/viewport validation in the seam. Do not add structural tree nodes. Verify: `cargo nextest run -p mbv 'test(/app::components::list/)'` covers collapse removing descendants while selection and viewport remain valid.

## 3. Shape implementations

- [ ] 3.1 Implement the flat-shape primitive adapters over `MediaList`/`WideMediaList`, replacing their retained-paint and ordered-mark storage with the shared carriers but deleting no arithmetic yet. Verify: `cargo nextest run -p mbv` is green with no existing test edits.
- [ ] 3.2 Confirm the flat shape remains behavior-neutral across every destination and shell composition path. Verify: `cargo nextest run -p mbv` is green; any existing assertion changed to make this step pass is a failure of behavior neutrality.
- [ ] 3.3 Define crate-visible `MusicTreeTarget::{Artist(ArtistKey), Album(String), Track { album: String, track: String }}` and an internal target↔arena-node map. Convert production APIs including hit resolution, selection, expansion, row rectangles, node-kind queries, and projected targets to `MusicTreeTarget`; keep `MusicNodeKey`, arena `usize`, and test-only arena helpers private to the tree module. A target absent from the current projection returns an explicit absent result. Verify: `cargo check -p mbv` is clean and `grep -RIn 'pub(in crate::app).*usize' src/app/components/music_tree*.rs` finds no production row-addressing API.
- [ ] 3.4 Update every non-tree caller, including `music_interaction.rs`, `music_content.rs`, `music_content_owner.rs`, `music_content_workspace.rs`, and affected tests/render characterization, to use `MusicTreeTarget`. Verify: `cargo check -p mbv` and `cargo clippy --workspace --all-targets -- -D warnings` are clean.
- [ ] 3.5 Delete arena-index assertions in `music_tree_tests.rs` and `music_tree_browser_tests.rs` and write focused tests against `MusicTreeTarget`; do not translate cases mechanically. Verify: `cargo nextest run -p mbv 'test(/music_tree/)'` is green.
- [ ] 3.6 Update `music_content_tree_tests.rs`, `render/tests_music_characterization.rs`, and `render/tests_music_groups.rs` to the target surface while preserving their painting contracts. Verify: `cargo nextest run -p mbv 'test(/music_(content_tree|characterization|groups)/)'` is green.
- [ ] 3.7 Implement the tree primitive adapters, shared retained-paint carrier, shared ordered marks, and `Expandable`, preserving hidden-filter mark aggregation and the crate-owned cursor/expansion authority. Verify: `cargo nextest run -p mbv 'test(/tests_tick_integration_music/)'` and `cargo nextest run -p mbv 'test(/music_tree/)'` are green.

## 4. Remove duplicate mechanics and evaluate diagnostics

- [ ] 4.1 Delete cursor, viewport, and keep-visible arithmetic from `media_list/` and the tree adapter, routing both through shared defaults while retaining only shape primitive adapters and named paging policy. Verify: `cargo nextest run -p mbv` is green and review finds no duplicate movement/clamping arithmetic in either shape.
- [ ] 4.2 Delete `WidePaintResult`, the tree's `paint_complete` field, and duplicate point-resolution state in favor of the shared retained-paint carrier. Verify: `cargo nextest run -p mbv` is green and `grep -RInE 'WidePaintResult|paint_complete' src/app/components/media_list src/app/components/music_tree*.rs` finds nothing.
- [ ] 4.3 Delete the tree's `selection_order` and the flat list's ordered-membership vector in favor of the shared ordered-mark carrier; keep flat-only Visual range/anchor fields local. Verify: `cargo nextest run -p mbv` is green and action-order tests still emit current display order.
- [ ] 4.4 Append the after production line total to `baseline.md` for the production files descended from the exact 1.1 list plus every new production file under `src/app/components/list/`; record the exact after file list so split files and the seam are included while tests are excluded. This is diagnostic, not a threshold: if the total is not lower, identify where the seam added lines, verify directly that every duplicated mechanic named in 4.1–4.3 is gone, and record why the result is justified or which duplication must still be removed. Verify: `baseline.md` contains the before/after file lists, totals, and completed follow-up analysis; line count alone cannot fail the change.
- [ ] 4.5 Run `cargo llvm-cov -p mbv --json --summary-only --output-path target/extract-shared-list-components-after.json` and append covered-line and count-line values for the exact after production file list from 4.4. Verify: aggregate line coverage across the governed subsystem, including the new seam, is at or above the 1.1 baseline; a shortfall is a blocker and requires added focused tests.

## 5. Verification and close-out

- [ ] 5.1 Confirm no seam type exposes a `tui-treelistview` type and `media_list` has no path to that crate. Verify: `grep -RIn 'tui_treelistview' src/app/components/list src/app/components/media_list` finds nothing, while dependency usage remains confined to the tree boundary.
- [ ] 5.2 Run the full gate: `cargo clippy --workspace --all-targets -- -D warnings`, `cargo fmt --all -- --check`, `cargo nextest run -p mbv`, and `cargo nextest run --workspace`. Verify: all commands are clean.
- [ ] 5.3 Confirm every file in the exact after production file list recorded by 4.4 is at or under 800 lines with `wc -l <recorded production files>`. Verify: no production file exceeds 800 lines; test files are governed by focused readability review rather than this production-file cap.
- [ ] 5.4 Run `openspec validate extract-shared-list-components --strict`. Verify: it passes.
- [ ] 5.5 Commit the implementation, revised planning artifacts, and `baseline.md`. Verify: `git status --short` is empty after the commit.
