# Tasks

## 1. Baselines and required file split

- [ ] 1.1 Create `openspec/changes/extract-shared-list-components/baseline.md` and record the pre-change production line total from `wc -l src/app/components/media_list/{mod,wide,carrier,anchor,selection,grouping}.rs src/app/components/music_tree{,_model,_view,_selection,_label}.rs`. Run `cargo llvm-cov -p mbv --json --output-path target/extract-shared-list-components-before.json` (full JSON, not `--summary-only`, so per-file entries are present); record, for exactly the production file list above, each file's covered-line and count-line values plus their sums, with the command. Verify: the markdown contains the command, file list, line total, and coverage numerator/denominator needed for the after comparison.
- [ ] 1.2 Split `src/app/components/media_list/mod.rs` into cohesive files under the 800-line cap, moving code only with no signature or behavior changes. Do not split an already-under-cap tree file unless work in this change would otherwise push it over 800 lines. Verify: `cargo check -p mbv`, `cargo nextest run -p mbv`, and `cargo fmt --all -- --check` are clean; review the diff as move-only.
- [ ] 1.3 Run `cargo clippy --workspace --all-targets -- -D warnings` and `cargo fmt --all -- --check`. Verify: both are clean before behavior work begins.

## 2. The shared seam

- [ ] 2.1 Define `RowFlow<Target>` in a new `src/app/components/list/` module: ordered rows, selectable-with-target versus structural-without, `len`, `row_at`, and `position_of(&Target)`. It has no dependency on `tui-treelistview` or `media_list` types. Verify: `cargo nextest run -p mbv -E 'test(/app::components::list/)'` covers structural rows occupying positions without being addressable.
- [ ] 2.2 Add `Cursored` with primitive selected-target read/write hooks and shared default methods for move, first, last, index, and `select_target`, skipping structural rows and clamping at both ends. Shape adapters may implement only the primitive hooks, not movement arithmetic. Verify: `cargo nextest run -p mbv -E 'test(/app::components::list/)'` covers structural skipping, clamp-no-wrap, absent targets, and deterministic resolution when the selected target vanishes from the flow.
- [ ] 2.3 Add `Viewported` (D1, D4) with primitive offset read/write hooks, shared clamp and keep-selection-visible defaults, and the two named paging policies exactly as they behave today: `FixedSelectableStride` (flat: `delta * 5` selectable rows, structural rows skipped) and `FixedVisibleStride` (tree: `delta * 5` projected rows). Also add the shared viewport-anchor carrier `ViewportAnchor<Target>` (selected stable target plus zero-based viewport row offset) with anchor production, restore-and-clamp application, and the discrete re-anchor entry point; `media_list` re-exports the carrier so existing `tv_content`/`library_panel` callers keep their paths. Verify: `cargo nextest run -p mbv -E 'test(/app::components::list/)'` covers offset preservation, minimum scroll, geometry clamping, both five-row paging policies, and anchor restore clamping at bounds.
- [ ] 2.4 Add the shared retained-paint state carrier and `PaintRetained` operations for begin, finish, invalidation, `claims_point`, `resolve_point -> Option<&Target>`, and `selected_row_rect`. Shape painters supply completed target-bearing geometry but no caller can access a row map. Verify: `cargo nextest run -p mbv -E 'test(/app::components::list/)'` covers invalid-state claims and stable-target resolution.
- [ ] 2.5 Add the shared ordered-mark state carrier and `MarkSelection`: add/remove retains addition order, while a shared row-flow projection returns selected targets in current display order for actions. Aggregation and flat-only Visual range/anchor state remain excluded. Verify: `cargo nextest run -p mbv -E 'test(/app::components::list/)'` covers interleaved addition/removal and click order differing from emitted action order.
- [ ] 2.6 Add `Expandable` (D5) as the tree-only adapter contract for stable-target expansion, parent/child movement, and aggregate mark state computed against a seam-owned `AggregateMark::{Unmarked, Partial, Marked}` vocabulary (the crate's `TreeMarkState` stays private to the tree adapter); keep expansion storage in `TreeListViewState` and shared post-change cursor/viewport validation in the seam. Do not add structural tree nodes. Verify: `cargo nextest run -p mbv -E 'test(/app::components::list/)'` covers collapse removing descendants while selection and viewport remain valid, expansion surviving content replacement by stable identity, and all three aggregate mark states.

## 3. Shape implementations

- [ ] 3.1 Implement the flat-shape primitive adapters over `MediaList`/`WideMediaList`, replacing their retained-paint and ordered-mark storage with the shared carriers and routing `media_list/anchor.rs`'s `ViewportAnchor` production/application through the seam's anchor operations (D1) with no behavior change to its `tv_content`/`library_panel` callers, but deleting no arithmetic yet. Verify: `cargo nextest run -p mbv` is green; existing test edits are limited to import/path updates for the moved `ViewportAnchor` type (the `media_list` re-export keeps callers unchanged).
- [ ] 3.2 Confirm the flat shape remains behavior-neutral across every destination and shell composition path. Verify: `cargo nextest run -p mbv` is green; any existing assertion changed to make this step pass is a failure of behavior neutrality.
- [ ] 3.3 Define crate-visible `MusicTreeTarget::{Artist(ArtistKey), Album(String), Track { album: String, track: String }}` (D2) and an internal target↔arena-node map, and add target-based production APIs for hit resolution, selection, expansion, row rectangles, node-kind queries, and projected targets alongside the existing node-id APIs (no caller changes yet; the node-id surface closes in 3.8). A target absent from the current projection returns an explicit absent result. Verify: `cargo check -p mbv` is clean and focused tree tests cover the target↔arena map including the explicit absent case.
- [ ] 3.4 Update every non-tree production caller — `music_interaction.rs`, `music_content.rs`, `music_content_owner.rs`, `music_content_workspace.rs`, `shell_music_workspace.rs`, `library_panel/panel_list.rs`, and `render/test_helpers_mounted.rs` — to use `MusicTreeTarget`. Verify: `cargo check -p mbv` and `cargo clippy --workspace --all-targets -- -D warnings` are clean (the node-id APIs still exist until 3.8, so unconverted test files keep compiling).
- [ ] 3.5 Delete arena-index assertions in `music_tree_tests.rs` and `music_tree_browser_tests.rs` and write focused tests against `MusicTreeTarget`; do not translate cases mechanically. Verify: `cargo nextest run -p mbv -E 'test(/music_tree/)'` is green.
- [ ] 3.6 Update every external suite that binds arena ids to the target surface while preserving its painting/composition contracts: `music_content_tree_tests.rs`, `music_content_artist_actions_tests.rs`, `music_content_artist_workspace_tests.rs`, `render/tests_music_characterization.rs`, `render/tests_music_groups.rs`, and the `tests_tick_integration_music*` suites; aggregate-mark assertions move to the seam's `AggregateMark` vocabulary (D5). Verify: `cargo nextest run -p mbv` is green.
- [ ] 3.7 Implement the tree primitive adapters, shared retained-paint carrier, shared ordered marks, and `Expandable`, preserving hidden-filter mark aggregation and the crate-owned cursor/expansion authority. Verify: `cargo nextest run -p mbv -E 'test(/tests_tick_integration_music/)'` and `cargo nextest run -p mbv -E 'test(/music_tree/)'` are green.
- [ ] 3.8 Close the tree boundary: make `MusicNodeKey`, the arena `usize` row-addressing APIs, and test-only arena helpers private to the tree module. Verify: `cargo check -p mbv` and `cargo clippy --workspace --all-targets -- -D warnings` are clean; `grep -RInE 'pub.*fn.*usize' src/app/components/music_tree.rs src/app/components/music_tree_model.rs src/app/components/music_tree_view.rs src/app/components/music_tree_selection.rs src/app/components/music_tree_label.rs` finds no production row-addressing API (the grep is a first pass — also re-read multi-line signatures and public fields by eye); and every public row API names `MusicTreeTarget`.

## 4. Remove duplicate mechanics and evaluate diagnostics

- [ ] 4.1 Delete cursor, viewport, and keep-visible arithmetic from `media_list/` and the tree adapter, routing both through shared defaults while retaining only shape primitive adapters and the named paging policies (D4). Verify: `cargo nextest run -p mbv` is green and review finds no duplicate movement/clamping arithmetic in either shape.
- [ ] 4.2 Delete `WidePaintResult`, the tree's `paint_complete` field, and duplicate point-resolution state in favor of the shared retained-paint carrier (D6). Verify: `cargo nextest run -p mbv` is green and `grep -RInE 'WidePaintResult|paint_complete' src/app/components/media_list src/app/components/music_tree*.rs` finds nothing.
- [ ] 4.3 Delete the tree's `selection_order` and the flat list's ordered-membership vector in favor of the shared ordered-mark carrier; keep flat-only Visual range/anchor fields local. Verify: `cargo nextest run -p mbv` is green and action-order tests still emit current display order.
- [ ] 4.4 Append the after production line total to `baseline.md` for every production file the 1.1 list's code ended up in (each surviving 1.1 file and every file it split into; if two merged or one disappeared, record which after file absorbed it) plus every new production file under `src/app/components/list/`; record the exact after file list so split files and the seam are included while tests are excluded. This is diagnostic, not a threshold: if the total is not lower, identify where the seam added lines, verify directly that every duplicated mechanic named in 4.1–4.3 is gone, and record why the result is justified or which duplication must still be removed. Verify: `baseline.md` contains the before/after file lists, totals, and completed follow-up analysis; line count alone cannot fail the change.
- [ ] 4.5 Run `cargo llvm-cov -p mbv --json --output-path target/extract-shared-list-components-after.json` (full JSON, not `--summary-only`) and append, for each file in the exact after production file list from 4.4, its covered-line and count-line values plus their sums. Verify: the after list's aggregate line-coverage ratio (summed covered lines ÷ summed count lines over the after file list, which includes the seam) is at or above the 1.1 baseline ratio computed over the 1.1 file list — each ratio over its own file set; a shortfall is a blocker and requires added focused tests.

## 5. Verification and close-out

- [ ] 5.1 Confirm no seam type exposes a `tui-treelistview` type and `media_list` has no path to that crate. Verify: `grep -RIn 'tui_treelistview' src/app/components/list src/app/components/media_list` finds nothing, while dependency usage remains confined to the tree boundary.
- [ ] 5.2 Run the full gate: `cargo clippy --workspace --all-targets -- -D warnings`, `cargo fmt --all -- --check`, `cargo nextest run -p mbv`, and `cargo nextest run --workspace`. Verify: all commands are clean.
- [ ] 5.3 Confirm every file in the exact after production file list recorded by 4.4 is at or under 800 lines with `wc -l <recorded production files>`. Verify: no production file exceeds 800 lines; test files are governed by focused readability review rather than this production-file cap.
- [ ] 5.4 Run `openspec validate extract-shared-list-components --strict`. Verify: it passes.
- [ ] 5.5 Record the new seam vocabulary (`RowFlow`, `Cursored`, `Viewported`, `PaintRetained`, `MarkSelection`, `Expandable`, `AggregateMark`, `ViewportAnchor`) in `CONTEXT.md` with one-line definitions. Verify: the terms appear with definitions; no existing term is renamed.
- [ ] 5.6 Sync the applied deltas into `openspec/specs/`: delete the three REMOVED requirements and apply the MODIFIED "WideMediaList owns fixed-row mechanics" in `openspec/specs/canonical-media-lists/spec.md`, and add the `shared-list-components` capability spec built from `specs/shared-list-components/spec.md`. Verify: `openspec validate --all` is clean and `openspec/specs/canonical-media-lists/spec.md` no longer contains "One shared owner supports list-local extension", "Responsive handoff preserves an explicit anchor", or "Canonical geometry has no compatibility path".
- [ ] 5.7 Commit the implementation, `baseline.md`, the checked-off planning artifacts, and the synced main specs. Verify: `git status --short` is empty after the commit.
- [ ] 5.8 Archive the change with `openspec archive extract-shared-list-components` (its planning artifacts, including `baseline.md`, move under `openspec/changes/archive/`). Verify: `openspec list --json` no longer reports the change as active and `openspec validate --all` stays clean.
