# Tasks

## 1. Spikes (block everything below)

- [x] 1.1 Confirm `const fn color(self) -> Color` with a full match compiles under MSRV 1.88 (const-match stable since 1.46, expected to pass) and that `serde_json` is available to `mbv` dev-dependencies (it is a workspace dep, expected to pass); record both outcomes in design.md and verify by pasting the check commands' output into the task trail
- [x] 1.2 Produce the 29-variant name table as `openspec/changes/palette-enum/name-table.md` (columns: old primitive(s) → new PascalCase variant → hex → one-word description) following the naming rules in design.md: keep existing color-words (`Gold`, `Aqua`, `Red`, `Orange`, `Purple`, `Foam`); number grey cluster (`Grey1`–`Grey6`) and dark green-grey cluster (`Green1`–`Green3`); blue-grey slates are `Slate`/`Storm`/`Flint`; plain hue names elsewhere; ordered by hue then dark-to-light. Get user review; verified by an approved table committed in the change

## 2. Palette core (additive, no callers)

- [x] 2.1 Add `render/theme/palette.rs` with the `Palette` enum, `const fn color()`, `ALL`, and hex/name accessors; verify `cargo check -p mbv` passes
- [x] 2.2 Add palette-value uniqueness test over `ALL` plus the `docs/palette.json` sync test; verify with `cargo nextest run -p mbv` for the new tests

## 3. Derive the roles and surfaces from the palette

Every row below is type-preserving: no role, surface row, level fill, or resolver
return changes type, so no call site, and in particular no frozen test file, is
affected. Each row's verification is an end state at a commit boundary — a green
tree — never an intermediate compile-error state.

- [x] 3.1 Rewrite the 51 role consts in `render/theme/mod.rs` (48 production, 3 `#[cfg(test)]`-gated; the test-only `resolve_surface_focus` fn is a fn, not a const, and derives the same way) from primitive aliases to `Palette` derivations — `pub(in crate::app) const ACCENT: Color = Palette::Aqua.color();` — using the variant assignment in `name-table.md` for each. Types stay `Color`; `HERO_META_ROLES` and `HINT_PILL_FILLS` stay `[Color; 3]` with each element derived. Reword each existing "deliberately not X" comment from primitive names to variant names, preserving the recorded intent verbatim in meaning (e.g. `PLAYBACK_TITLE_FG` is "its own role, deliberately not `ACCENT`, whose `Palette::Aqua` value it shares today"); delete no comment. Verify: `cargo check -p mbv` clean, `cargo nextest run -p mbv` fully green, `git diff --stat` shows `theme/mod.rs` and nothing outside `src/app/render/theme/`
- [x] 3.2 Derive the surface tier from the palette at all three of its production sites: `Level::focused_fill`/`resting_default` in `surface.rs`, every `surface_table.rs` row value, **and** the inline `primitives::SOFT_CONTENT_BODY_BG` in `surface_resolve.rs:52` (the `soft` branch of `surface_colors`) — the one production `primitives::` reference outside the rows, which becomes `Palette::Green2.color()` (same `#48584e`). Each becomes `Palette::X.color()` or a role const that already is one. Verify no production `primitives::` reference remains: `rg 'primitives::' src/app/render/theme/` matches only `#[cfg(test)]` code. `Row.resting`, `focused_fill`, `SurfaceColors.fill` and `surface_colors` keep their `Color` types — `PopupDimBackdrop`'s `DIM_BLEND_BASE` (`Color::Black`) stays an ordinary row value with its existing rationale comment. Verify: `cargo check -p mbv` clean, `cargo nextest run -p mbv` fully green, `git diff --stat` confined to `src/app/render/theme/`
- [x] 3.3 Delete `render/theme/primitives.rs` and its `mod primitives;` wire once no const references it; gate `Palette::ALL`, `name()` and `hex()` behind `#[cfg(test)]` (they are test-only consumers — structurally, never `#[allow(dead_code)]`). Verify: `rg 'Color::Rgb' src/app/render/theme/` matches only `palette.rs`; `cargo check -p mbv` emits no warnings; `cargo nextest run -p mbv` fully green; `cargo clippy --workspace --all-targets -- -D warnings` clean

## 4. Cleanup and proof

- [ ] 4.1 Migrate the one stray production `Rgb` literal: `src/app/render/components/chrome_tabs.rs:168` `Color::Rgb(73, 81, 86)` → `palette::PILL_FG` (same `#495156`). The test-only literals in `library_hero_overlay.rs` and `media_list.rs:413,478` (confirm the line numbers against HEAD before writing them down) are buffer assertions, not palette colours — document them as known residual in `theme/palette.rs`'s module doc, do not migrate them. Verify: `rg 'Color::Rgb' src/ --glob '!**/theme/palette.rs'` matches only the documented test residual and `backdrop.rs`'s dim arithmetic; `cargo nextest run -p mbv` fully green
- [ ] 4.2 Sync `docs/palette.json` with the enum and prove value parity against base `69344aff`: for every one of the 46 production roles and all 34 surface rows, the resolved value is byte-identical to the base tree's. Verify: the `docs_palette_json_matches_the_palette` test passes; a role→hex and surface-row→hex comparison against `69344aff` shows zero differences. Run it mechanically: `git show 69344aff:src/app/render/theme/primitives.rs | rg -o 'Rgb\([^)]*\)' | sort -u` against the enum's `color()` arms for the literal set, and add a temporary `#[cfg(test)]` comparison that asserts each of the 46 production roles and all 34 `surface_colors` results equals the hex recorded in `docs/palette.json`; paste both outputs into the task trail, then delete the temporary test; full `cargo nextest run -p mbv`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo fmt --all -- --check`. `docs/palette.html` is unchanged — its role-first layout still matches the JSON's role listing, which stays hand-maintained (no generator exists and the repo forbids adding one)
