# Fix Podcast Media-List Ownership Tasks

## 1. Characterize podcast ownership and presentation

- [x] 1.1 Add or update focused podcast component/media-list tests proving persistent-control-owned movement and selection, stable-target ordinary refresh with local clamp, retained show-hit resolution and invalidation with blank/heading no-ops, retained-claim-gated wheel behavior, and bidirectional `ViewportAnchor` handoff between controls preserving target plus selected-row offset; verify the targeted component/media-list tests pass without `show_rows`, `columns`, parent scroll, or painted-offset assertions.
- [x] 1.2 Add and register `src/app/tests_tick_integration_podcast.rs` with Wide AND Normal/Narrow coverage through `Application::tick()`, `draw_frame`, and the shell sync pass proving navigation paints the same selected show, clicks resolve painted shows while blank/header clicks are no-ops with discriminating empty/non-empty `raw_messages` assertions, wheel input is claimed only over the active control with both on-control and off-control legs, exactly one list painter paints each frame with no base-frame underpaint, and a breakpoint round-trip preserves target plus selected-row offset; verify the targeted shell-tick tests pass.
- [x] 1.3 Extend podcast render-buffer tests with spec-delta-conformant evidence that Wide and inline selected-show detail render episode filter pills and matching episode rows, episode rows show canonical duration plus played/active semantic states, show rows remain duration-free Collections, Wide hero renders the show title, and pill labels follow the accepted character-safe 18-character truncation/overflow contract while bucket pills remain non-empty surname ranges without `All` or `#`; verify the targeted render tests pass at representative Wide and Normal/Narrow dimensions.

## 2. Transfer podcast list and presentation authority

- [x] 2.1 Refactor `AudiobookshelfPodcastComponent` to add persistent `wide_show_list: WideMediaList<String>` beside the existing persistent narrow control; make only the active show control own cursor, scroll, movement, selection, retained geometry, and wheel claims; transfer responsive state control-to-control through one `ViewportAnchor`; and delete the parent `scroll`, `columns`, `show_rows`, `selected_row_offset`/`painted_row_offset`, content reseed, and per-frame Wide-control construction paths; keep `AudiobookshelfBrowseState.selected_id`/`cursor()`/`select()` effect-path-only and verify focused tests cover control-owned movement and handoff.
- [x] 2.2 Refactor the podcast shell/render seam so show clicks use the active control's `claims_current_point`/`resolve_current_point`, wheel moves only a claiming active control and returns `MouseClaimed`, show paints retain their own current-frame geometry, and shell copies into `app.layout.main.*` are narrowed to rects still required by shell-owned chrome/image/overlay or podcast episode-workspace readers; preserve event-scoped projection, component-resolved `select_audiobookshelf_show`/`save_audiobookshelf_position` effects, the intentional `AudiobookshelfPodcastEpisodeTransition` no-op, episode workspace authority, browser-left/hero-right orientation, and one painter per breakpoint; verify retained-hit and no-underpaint tests pass.
- [x] 2.3 Converge podcast presentation through existing shared helpers: project episode `duration_seconds` with canonical `M:SS`/`H:MM:SS` formatting and played/active row states, keep show Collection rows duration-free, render episode rows plus `All`/`Played`/`Unplayed` pills in inline selected-show detail without changing filter/selection intent state, enable the Wide hero title, and apply shared pill chrome and character-safe label truncation while leaving non-empty surname-range bucket data unchanged; verify the spec-delta render-buffer evidence passes without forking shared helpers.

## 3. Verify the bounded repair

- [x] 3.1 Run the relevant podcast component, media-list, render-buffer, shell, and shell-tick tests at Wide and Normal/Narrow breakpoints; verify retained hits and invalidation, active-only navigation/wheel claims, target-plus-offset handoff, spec-delta inline presentation, the base frame reserving without underpainting, and exactly one list painter per frame.
- [x] 3.2 Run `cargo fmt --all -- --check`, `cargo check -p mbv`, relevant `cargo nextest run -p mbv`, `cargo clippy --workspace --all-targets`, and `ast-grep scan`; record any unrelated failure without widening this change.
