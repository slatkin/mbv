## 1. Audit Live and Smoke Tests Across the Workspace

- [x] 1.1 Search every workspace test and test-support target for real mpv initialization, TCP/Unix listeners or streams, loopback HTTP servers, product-process or binary spawning, writable config/state/temp directories, symlink/filesystem preparation, and live Service clients; record each candidate and its delete/convert/retain disposition in this task's completion note, and verify every retained candidate explains why it constructs no prohibited external.
  - note: `src/app/input_music_track_test_support.rs:RecursiveFetchServer` — delete; unused loopback HTTP harness with no callers.
  - note: `crates/mbv-core/src/cast_client.rs:connect_failure_returns_error_not_panic` — delete; assertion only probes an OS TCP refusal.
  - note: `crates/mbv-core/src/applog.rs:push_entry_writes_to_file` — delete; assertion only probes filesystem logging, not mbv-owned bookkeeping.
  - note: (rule-1) `crates/mbv-core/src/api_tests_client.rs:device_id_*` — delete; subject is filesystem device-id migration in writable temp state, not mbv-owned logic.
  - note: (rule-1) `src/single_instance.rs` `resolve()` Fresh/Refuse/Attach decision table — delete; subject is flock per-open-file-description/listener OS-facility semantics.
  - note: (rule-1) `crates/mbv-core/src/cast_client.rs:connect_failure_returns_error_not_panic` — delete; stimulus is an OS TCP refusal; surviving classification coverage lives in the mock-based `map_error`/service-failure tests.
  - note: `crates/mbv-core/src/audiobookshelf_playback_tests.rs` and `api_failure_tests.rs` — retain; loopback-looking URLs are passed through `MockHttp`/in-memory ureq transport, no socket or live Service is constructed.
  - note: `crates/mbv-core/src/daemon_tests_ctrl_auth.rs` — retain; `UnixStream::pair` is an in-memory synchronization primitive, not a listener or filesystem socket.
  - note: `crates/mbv-core/src/remote_player_tests.rs`, `src/app/**` URL/endpoint tests — retain; endpoint strings are parsed/formatted only and no connection is made.
  - note: `crates/mbv-core/src/*config_tests*`, app `TestStateDirGuard` users — retain for this bounded audit; guarded filesystem writes exercise mbv persistence/bookkeeping and are isolated by the existing test seam.
  - note: `crates/mbv-core/examples/audiobookshelf_contract_probe.rs` — retain; explicit controlled manual probe example, not a test target.
  - note: `src/app/tests_library_position_restore.rs` — retain/convert: `restoring_pre_pill_feature_position_captures_library_total_and_shows_pills` and the guarded `restored_default_library_fallback_rewrites_state_file_after_success`/`stale_restore_is_ignored_*` persistence tests restored as-is (TestStateDirGuard seam); `restoring_library_position_does_not_eagerly_prefetch_all_items` restored with the socket canary dropped (no-prefetch claim asserted hermetically).
  - note: `src/app/tests_remote_commands.rs` — convert: `session_item_change_updates_state_without_mutating_queue` and `remote_jump_target_resolves_next_and_previous_without_tracking` restored socket-free; the request-formation/error-classification tests drive the same POST endpoints, body fields, and `SessionEvent::CommandError` mapping through the existing in-memory `MockHttp` ureq transport; the removed TCP stub-daemon handshake (rule-1) had no mbv-owned claim beyond these.
  - note: `src/app/tests_route_state.rs` #233 disconnect-on-swap tests — convert: remote→remote swap, route→route swap, and restore-to-local disconnect now drive the real shell paths over an in-memory `UnixStream` pair via the `connect_stub_daemon_pair` test-support seam (the peer observing EOF is the oracle); no listener or spawned process remains.
- [x] 1.2 Delete candidates whose assertions test an external, operating-system facility, smoke path, or harness behavior rather than mbv-owned logic; remove helpers used only by those tests, and verify each affected package's `cargo nextest run -p <package>` invocation passes.
- [x] 1.3 Convert live-harness tests that protect mbv-owned request formation, error classification, lifecycle decisions, or bookkeeping to an existing in-memory/mock boundary; add only the narrowest `cfg(test)` injection seam if no existing boundary can drive the claim, and verify the converted tests reproduce the original mbv-owned assertions without sockets, processes, writable filesystem state, live Services, or real mpv handles.
- [x] 1.4 Repeat the workspace structural searches from 1.1 after cleanup, reconcile every remaining hit against the recorded inventory, and verify that only read-only static fixtures, pure path/string logic, in-memory synchronization, or other documented hermetic uses remain.
  - note: Re-ran `TcpListener|TcpStream|UnixListener|UnixStream|init_mpv|test_mpv|MpvHandle|Command::new|process::Command|tempdir|TempDir|temp_dir|symlink` over test/test-support targets: remaining hits are production call sites (e.g. `daemon_run.rs` bind, `player_runtime.rs` mpv init/symlink, `feed_actions.rs`/`notify_actions.rs` process spawn) plus hermetic test uses; every remaining test-side hit reconciles with a recorded retain/convert disposition (`MockHttp` transport, `UnixStream::pair`, guarded `TestStateDirGuard`/`TestTempDir` writes, read-only fixtures, pure endpoint/path parsing). No unrecorded candidate remains.

## 2. Establish the Bounded Presentation Audit Set

- [x] 2.1 Inventory the ten ignored legacy render tests by name (`wide_both_columns_panels_and_chrome_follow_the_table`, `wide_library_only_hero_and_rail_follow_the_table`, `mini_view_halves_follow_the_table`, `wide_music_browser_container_is_occluded_by_the_shell`, `narrow_grouped_music_keeps_bottom_hero_fully_visible`, `narrow_grouped_music_persists_bottom_hero_scroll`, `short_window_keeps_queue_in_left_column`, `wide_active_queue_starts_below_panel_rows`, `wide_queue_only_leftover_rows_stay_dark_bg_without_duplicate_visualizer`, and `tab_bar_and_status_row_legacy_base_frame_publish_placements_but_paint_no_panel`) plus the ignored obsolete boundary test `tick_queue_boundary_drag_live_width_then_persists_once_on_release`; verify all eleven definitions are found or record the renamed/current equivalent before changing them.
  - note: All eleven exact definitions resolved: `src/app/render/tests_surface_conformance.rs:177` wide_both_columns_panels_and_chrome_follow_the_table; `:323` wide_library_only_hero_and_rail_follow_the_table; `:464` mini_view_halves_follow_the_table; `:551` wide_music_browser_container_is_occluded_by_the_shell; `src/app/render/tests_music_groups.rs:122` narrow_grouped_music_keeps_bottom_hero_fully_visible and `:191` narrow_grouped_music_persists_bottom_hero_scroll; `src/app/render/tests_queue.rs:103` short_window_keeps_queue_in_left_column, `:613` wide_active_queue_starts_below_panel_rows, and `:638` wide_queue_only_leftover_rows_stay_dark_bg_without_duplicate_visualizer; `src/app/render/tests.rs:234` tab_bar_and_status_row_legacy_base_frame_publish_placements_but_paint_no_panel; `src/app/tests_tick_integration_mouse_panels.rs:52` tick_queue_boundary_drag_live_width_then_persists_once_on_release. Each has an `#[ignore]` marker with the stated obsolete reason; no renamed equivalent was needed.
- [x] 2.2 Inventory whole-frame equality assertions, frame-glyph locators, mounted tests that repeat appearance, and the six row-sensitive cases named in #707 or their current equivalents; for every assertion proposed for removal, record in this task's completion note either the exact surviving owner test/assertion or that the detail is intentionally no longer a contract.
  - note: No whole-frame string equality assertion was found in the bounded render set; `buffer_to_string` uses are focused `contains`/line or region extraction. The owner-layer candidates are: arrangement relational rect assertions in `src/app/render/arrangements/chrome.rs:551-607` and `src/app/render/arrangements/queue.rs:183-194` (keep at arrangement); Render Component focused content/role checks in `src/app/render/tests_home_characterization.rs:39-193`, `src/app/render/tests_library_characterization.rs:24-149`, `src/app/render/tests_music_groups.rs:101-119`, and `src/app/render/tests_queue.rs:320-572` (keep or narrow at Render Component); Interactive Component retained geometry/target checks in `src/app/components/media_list/tests.rs` and `src/app/tests_tick_integration_library_panel.rs:309-594` (keep at Interactive Component); shell tick integration checks in `src/app/tests_tick_integration_mouse_panels.rs:202-384`, `src/app/tests_tick_integration_home.rs:253-345`, `src/app/tests_tick_integration_browser.rs:51-150`, and `src/app/tests_tick_integration_music_mouse.rs:15-249` (keep only routing/latest-frame/stable-target claims). Frame-glyph locator candidates are `src/app/render/tests.rs:105-187` (next-glyph width/position), `src/app/render/tests_queue.rs:399-431` (seekbar glyph as transport semantic output: keep at painter owner), and the ignored legacy visualizer scan at `src/app/render/tests_queue.rs:638-709` (duplicate/obsolete; no surviving owner needed beyond queue playback painter coverage).
  - note: The six #707 names are absent under those exact names. Current owner equivalents/evidence are: `home_buffer_characterization_covers_wide_unfocused_narrow_and_selected_states` -> `src/app/render/tests_home_characterization.rs:39-193` (`startup_frame_paints_loading_affordances_not_blank_panes`, `home_pill_row_and_targets_are_characterized_end_to_end`, and focused/narrow hero tests; Render Component); `movies_plain_replacement_characterization_covers_bottom_scroll_fallback_and_targets` -> no exact current characterization (the source comment at `src/app/render/tests_library_characterization.rs:32-35` records the legacy Movies test as deleted); the current fallback owner is `src/app/render/tests_music_groups.rs:227` and current Movies latest-frame owner is `src/app/tests_tick_integration_mouse_panels.rs:202`; `matrix_cannot_fit_preserves_an_ordinary_selected_row` -> `src/app/render/tests_music_groups.rs:227-306` (`short_grouped_music_restores_the_ordinary_selected_album_row`) and `src/app/render/tests_conformance_matrix.rs:371-407` (Render Component/arrangement matrix evidence); `library_panel_wheel_persists_owner_resolved_scroll` -> no exact current name, with wheel/owner evidence in `src/app/tests_tick_integration_library_scroll.rs:20-80` (`deferred_wheel_is_drained_before_following_keyboard_cursor_move`) and `:82-117` (`library_panel_wheel_at_loaded_edge_fetches_next_page`); `music_narrow_album_uses_retained_geometry_for_live_mouse_gestures` -> current equivalent `src/app/tests_tick_integration_mouse_panels.rs:307-384` (`music_click_resolves_current_retained_geometry_through_application_tick`) plus `src/app/tests_tick_integration_music_mouse.rs:15-113` (track retained geometry); `music_narrow_album_activation_opens_the_track_selection_modal` -> no exact current name, with activation/modal boundary evidence in `src/app/tests_tick_integration_music.rs:167-221` and `src/app/shell_selection_modal_tests.rs:311-357`. No assertion is proposed for removal in Unit 2; Units 3-5 must cite these surviving owner proofs or explicitly record intentional de-contracting.
- [x] 2.3 If the inventory leaves row-budget coupling unclear, temporarily add or remove one spacer row in the shared hero/list path, run the affected `mbv` test families to collect failures, then restore the production change completely; classify every failure using the evidence rule from 2.2 and verify `git diff` contains no production rendering change before continuing.
  - note: Skipped. Row-budget ownership is clear from existing evidence: `src/app/render/tests_library_characterization.rs:125-149` proves the library rail/status gap at the arrangement/panel boundary; `src/app/render/tests_music_groups.rs:101-119` and `:227-306` prove admitted-hero versus ordinary-row fallback; `src/app/render/tests_conformance_matrix.rs:371-407` proves selected-row swallowing; and the mounted tests above resolve retained geometry from the latest painted frame. A production spacer perturbation was therefore unnecessary and would add no information before the planned owner-level edits. No production file was changed.

## 3. Remove Inert Presentation Debt

- [x] 3.1 Delete the ten named ignored legacy render tests from 2.1, remove only imports and local helpers made unused by those deletions, and verify those ten function names and their legacy ignore reason are absent while the affected render test modules compile and pass.
- [x] 3.2 Delete the named ignored obsolete boundary test from 2.1, remove only scaffolding made unused by that test, and verify the remaining mouse-panel tick integration tests pass through `Application::tick()`.

## 4. Right-size Arrangement and Render Component Tests

- [ ] 4.1 Refine arrangement tests in the bounded hero/list families so each owned placement or breakpoint fact is asserted once and relationally; delete an assertion only when its 2.2 completion evidence identifies the surviving owner or deliberate de-contracting, and verify the affected arrangement tests pass at the relevant Narrow and Wide boundaries.
  - note: Reviewed `src/app/render/arrangements/chrome.rs:551-607` and `src/app/render/arrangements/queue.rs:183-194`; placement assertions remain at their arrangement owner and were not duplicated or deleted. Narrow/Wide arrangement coverage passed in the targeted render family run.
- [ ] 4.2 Own the #707 cases `home_buffer_characterization_covers_wide_unfocused_narrow_and_selected_states`, `movies_plain_replacement_characterization_covers_bottom_scroll_fallback_and_targets`, and `matrix_cannot_fit_preserves_an_ordinary_selected_row` (or their current equivalents) exclusively in this group: narrow them to semantic content, style role, paint-local geometry, or direct fit/fallback predicates; remove whole-frame equality and glyph locators; derive fixture capacity from named requirements; and verify each test directly exercises its named Render Component or arrangement branch.
  - note: `src/app/render/tests_home_characterization.rs:home_pill_row_and_targets_are_characterized_end_to_end` — removed the full-frame scan for every `◢` row; surviving owner proof is the selector painter's retained target row plus focused label/style assertions in the same test (2.2 owner candidate).
  - note: `src/app/render/tests_library_characterization.rs` — Movies characterization has no current test; the legacy deletion is recorded at lines 32-35. Fallback/target coverage survives in `src/app/render/tests_music_groups.rs:short_grouped_music_restores_the_ordinary_selected_album_row`; latest-frame Movies coverage survives in `src/app/tests_tick_integration_mouse_panels.rs:202` (not edited).
  - note: `src/app/render/tests_music_groups.rs:narrow_grouped_music_replaces_selected_album_row_with_hero_detail` — removed the glyph locator at the hero edge; surviving owner proof is selected-target containment in the admitted hero and semantic title/target assertions.
  - note: `src/app/render/tests_music_groups.rs:short_grouped_music_restores_the_ordinary_selected_album_row` — removed redundant rectangle inequality; direct fallback proof remains `hero_area == Rect::default()`, selected target presence, and one published target.
  - note: `src/app/render/tests_conformance_matrix.rs:matrix_bottom_selected_heroes_swallow_their_source_rows` — replaced exact x/y/width and upward-growth assertions with relational target containment; semantic title count remains the surviving owner proof.
- [ ] 4.3 Refine the remaining bounded Render Component tests to retain glyph assertions only where the glyph is the painter's own semantic output and to use focused buffer/content assertions instead of whole frames; delete each claim only with its 2.2 evidence, and verify the affected buffer tests pass.
  - note: `src/app/render/tests.rs:title_row_paints_the_plain_next_control` and `title_row_paints_the_nerd_font_next_control` — renamed from position-locator names and removed glyph-derived x assertions; painter-owned glyph presence and paint-local width remain.
  - note: `buffer_to_string` remains in use for focused semantic content checks; no helper deletion was warranted.

## 5. Narrow Interactive and Mounted Integration Tests

- [ ] 5.1 Narrow affected Interactive Component tests to local state transitions, semantic requests, viewport behavior, and retained hit resolution; remove assertions that repeat parent spacing or painter decoration only with their 2.2 evidence, and verify the affected component test modules pass.
- [ ] 5.2 Own the #707 cases `library_panel_wheel_persists_owner_resolved_scroll`, `music_narrow_album_uses_retained_geometry_for_live_mouse_gestures`, and `music_narrow_album_activation_opens_the_track_selection_modal` (or their current equivalents) exclusively in this group: retain their `Application::tick()`, latest-frame gesture, stable-target, and modal/effect claims; remove repeated glyph, spacing, and breakpoint-proxy assertions; derive only the fixture capacity needed for the integration claim; and verify all three pass in their relevant Narrow or Wide presentation.
- [ ] 5.3 Narrow other affected mounted tick and mouse tests to mounting, focus, subscriptions, latest-frame delivery, routing, stable-target resolution, and cross-boundary effects; delete each presentation claim only with its 2.2 evidence, and verify the targeted integration modules still pass through the real shell sync and `Application::tick()` path.
- [ ] 5.4 Remove shared test helpers and fixtures left with no callers after groups 3-5, keep `buffer_to_string` where focused semantic-content assertions still use it, and verify `cargo check -p mbv --tests` reports no dead imports or compilation failures.

## 6. Prevent Regression and Verify

- [ ] 6.1 Update the Tests section and completion checklist in `.agents/skills/mbv-frontend/SKILL.md` with the four-layer ownership matrix, deletion-evidence requirement, terminal-size intent rule, owner-only semantic glyph rule, whole-frame equality prohibition, and mounted-test integration boundary; verify the guidance requires no checker, snapshot system, live fixture, or one-test-per-deletion replacement.
- [ ] 6.2 Run `cargo fmt`, `cargo nextest run --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, and `openspec validate right-size-tui-presentation-tests`; verify all pass with no prohibited live external construction, production behavior change, or uncommitted temporary perturbation.
- [ ] 6.3 Summarize live/smoke dispositions and presentation assertions deleted, converted, narrowed, and retained by owning layer for #707/#697; name the surviving owner proof or deliberate de-contracting for every presentation deletion, record any informational coverage remeasurement separately on #697 if one was run, and verify no new shell/effect/run-loop coverage was added merely to replace removed tests.
