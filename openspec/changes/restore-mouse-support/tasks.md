Each task is tagged **[D]** (design-bearing — needs judgement, route to a strong
model) or **[M]** (mechanical — the shape is fixed by design.md). Phase gates
from Phase 3 onward verify **both breakpoints**: narrow is where hit geometry
differs most, and two surfaces in Phase 5 exist only there.

## 1. Foundations (ADR + ledger seam)

- [x] 1.1 **[D]** Write `docs/adr/0024-mouse-events-through-component-subscriptions.md` recording D1 (any-position subscription + the pinned tuirealm 4.1 `is_in_range` assumption + component-side kind filtering), D2 (eligibility decided before delivery, not by discarding messages), D3 (mounted-parent gesture recognition), D11 (mouse eligibility follows paint, and why that is not ADR 0022 debt), and D13 (Residual A closed here) as Accepted; verify it cross-links ADR 0022/0023 and `openspec validate restore-mouse-support --strict` still passes.
- [x] 1.2 **[M]** Replace the "Mouse ownership is out of scope" section of `docs/architecture/interactive-surface-ledger.md` with "Mouse owner / gestures / breakpoints verified / verification" columns on every row (value `pending` for all); verify the table still renders and `rtk make check-code-file-lines` passes for the doc.

## 2. Phase 1 — Delivery spine, eligibility, and Residual A geometry

_Phase 1 lands delivery with **no new gestures and no per-surface behaviour
change** (D12): the four `if !self.focused { return None }` guards in
`feeds.rs:322`, `audiobookshelf_book.rs:214`, `audiobookshelf_podcast.rs:204`,
`home.rs:385` stay until Phase 3. The phase's one intended observable win is
`PlaybackComponent`, which has no focus guard, so it is also what the delivery
tests assert against._

- [x] 2.1 **[M]** Add a `mouse_sub()` helper returning `Sub::new(EventClause::Mouse(MouseEventClause { kind: <any>, modifiers: <any>, column: 0..u16::MAX, row: 0..u16::MAX }), SubClause::Always)`. Note the half-open `Range<u16>` — `0..=u16::MAX` does not type-check. Unit-test that the clause forwards a `MouseEvent` of **every** `MouseEventKind` at arbitrary coordinates, so the test fails loudly if a future `tuirealm` starts honouring `kind`. Add a comment above `mouse_sub()` naming the tuirealm 4.1 pin and pointing at ADR 0024.
- [x] 2.2 **[M]** Attach `mouse_sub()` at every interactive-component mount site in `src/app/shell.rs` and the lazy mount paths (`mount_home`, `mount_feeds`, `reconcile_destination_mounts`, overlay/popup mounts, `Playback`); verify `rtk cargo check -p mbv` passes.
- [x] 2.3 **[D]** Add `sync_mouse_subscriptions()` in `src/app/shell_library.rs` beside `sync_active_destination` (D2), driven off the same `library_child_id()` derivation, and call it in the same synchronisation pass. It `subscribe`s/`unsubscribe`s `mouse_sub()` so the eligible set is exactly: a mounted blocking overlay alone; else the topmost mounted panel-covering overlay/popup alone (`root.rs` `OVERLAY_IDS` z-order); else the components painted this frame (active destination, Queue, Playback, chrome). Every other mounted destination is unsubscribed. Unit-test the eligible set for each of the three branches.
- [x] 2.4 **[D]** Add the mouse-message fold to `src/app/shell_run.rs` beside the keyboard router fold: take the mouse-derived messages out of a `tick()`, apply at most one (first in `tick()` order), and `debug_assert!` that no more than one was produced — eligible surfaces paint disjoint regions, so two claims is a geometry defect, not a ranking problem (D2). Do **not** add a `Msg`-variant→surface rank table. Unit-test one-message and (debug) two-message cases.
- [x] 2.5 **[M]** Retire `TerminalObserverEvent::Mouse`: remove the drop arm at `shell.rs:349`, the `root.rs:72` mapping, and the enum variant; verify `rtk cargo check -p mbv` passes and no `match` regresses to a wildcard.
- [x] 2.6 **[M]** Route the folded mouse message through the existing `handle_terminal_message` dispatch so the five landed surfaces + `PlaybackComponent` act from any focus state.
- [x] 2.7 **[D]** Close ADR 0022 Residual A site `mouse_gestures.rs:27`: change `PlaybackRequest::SeekTo(u16)` to `SeekTo(f64)` carrying a fraction resolved by `PlaybackComponent` against its own `seekbar_area` (`playback.rs:193`, emitted at `playback.rs:147`), and replace `App::seek_to_col` with `seek_to_fraction(f64)` that no longer reads `self.layout.playback.seekbar_area` (`shell_playback.rs:107`). Verify by an integration test through `tick()`: with another component focused, an injected click on the seek bar produces `Msg::Playback(PlaybackRequest::SeekTo(f))` with the expected fraction, and transport-button clicks produce their intents. This is also Phase 1's delivery proof — it asserts an emitted `Msg`, not merely that a handler ran.
- [x] 2.8 **[D]** Close ADR 0022 Residual A site `input.rs:97`: `ensure_tab_visible` reads the painted `self.layout.tabs_area.width` on a **keyboard** path. Extract the tab-bar width derivation from `render/arrangements/chrome.rs:113` (terminal width + right-panel visibility) into a pure arrangement function and call it from both the painter and `ensure_tab_visible`, so the shared layout invariant lives in one arrangement primitive rather than being re-derived. Verify no read of `layout.tabs_area` remains outside the painter, and the existing tab-scroll tests stay green.
- [x] 2.9 **[D]** Land the eligibility and focus invariants as tests: (a) the eligible set matches the painted set across a destination switch, a wide/narrow breakpoint change, and an overlay mount/unmount; (b) a mouse event over a painted destination leaves every other mounted destination's cursor/scroll/selection untouched (their handlers are never invoked); (c) a mounted blocking overlay holds keyboard focus when the synchronisation pass completes — the one accepted bypass in D2 depends on it.
- [x] 2.10 **[M]** Phase-1 gate: `rtk cargo nextest run -p mbv`, `rtk cargo clippy --workspace --all-targets`, `rtk cargo fmt --check`; confirm no new gestures and no per-surface behaviour change beyond `PlaybackComponent` becoming reachable — the four focus guards are still in place.

## 3. Phase 2 — Shared primitives

_This change owns the full mouse migration. Nothing is deferred to another
change. Per D6 the two canonical controls get point resolution as a method built
on their existing exported row flow, **not** a rect registry; `HitRegions<Tag>`
exists for irregular painted chrome. This scope note also governs Phase 3._

- [x] 3.1 **[M]** Create `src/app/components/mouse/hit.rs` with `HitRegions<Tag>` (`clear`, `push(rect, tag)`, `resolve(point) -> Option<&Tag>`, last-push-wins) for **irregular painted chrome only** — pills, Queue scope buttons, transport controls, group selectors, overlay rows. Unit-test overlap resolution and empty/out-of-bounds cases.
- [x] 3.2 **[D]** Create `src/app/components/mouse/gesture.rs` with `MouseGestureState` consuming raw `MouseEvent`s and emitting `Click`/`DoubleClick`/`RightClick`/`Scroll` (reserve `DragStart`/`DragMove`/`DragEnd`/`HoverEnter`/`HoverLeave`); it owns the double-click interval and wheel throttle per parent. Cost the throttle against the run loop's `PollStrategy::Once(50ms)` cadence and record the chosen intervals with that reasoning in the module comment. Unit-test the double-click window and wheel throttle in isolation.
- [x] 3.3 **[D]** Add `resolve_point` to the already-landed controls, built on their existing `row_geometry` export (`wide.rs:100`, `inline.rs:170`) — no stored per-row rect list: `WideMediaList::resolve_point(&self, list_area: Rect, point: Position) -> Option<&Target>` and `InlineMediaBrowser::resolve_point(&self, list_area: Rect, detail_rows: usize, point: Position) -> Option<&Target>`, the parent passing the rectangle and detail-row count it painted. Unit-test resolution against a scrolled viewport, a heading row (no target), an inline detail block, and out-of-bounds points.
- [x] 3.4 **[D]** Migrate the mounted Browser parent onto `MouseGestureState`; resolve list rows through `resolve_point`, delete the `*HitRegion` enum, hold parent-owned non-list chrome (pills, chrome wheel, right-click recognition + anchor) in `HitRegions<Tag>`, and keep parent click-to-focus. Verify row click/select/activate/scroll and parent-owned control delivery.
- [x] 3.5 **[D]** Same migration for the Home and Queue mounted parents, deleting `QueueHitRegion` and Home's row enum, and holding Queue's scope buttons in `HitRegions<Tag>`. Queue is small — `queue.rs:434` already rebuilds row rects from `list.resolve_viewport`, so the row path collapses into `resolve_point`.
- [x] 3.6 **[D]** Same migration for the TV and Music mounted parents: TV's row enums deleted; Music's **wide right-rail** rows resolved through `resolve_point` (Music has no `*HitRegion` enum to delete). Music's narrow list and track table are net-new work in Phase 5 (task 6.1).
- [x] 3.7 **[M]** Delete each per-surface `*HitRegion` row enum as its surface is migrated above, and delete the "shell decides single vs double" contract text in `msg/hit_regions.rs`. **Do not delete `note_browse_double_click`, `note_browse_scroll`, or `App.last_click_time`/`last_click_pos`/`last_scroll_at` here** — surfaces migrated in Phases 3 and 5 still consume them; they go in task 6.3 once the last consumer is gone. Verify `rtk cargo check -p mbv` passes.
- [x] 3.8 **[M]** Phase-2 gate: full `rtk cargo nextest run -p mbv` green with zero characterization-buffer changes (hit geometry is never painted); confirm this phase added no observable behaviour. If review finds an observable pointer/rendered-UI change, treat it as a bug and rerun the affected tests and gate before phase acceptance.

## 4. Phase 3 — Main-surface parity, wheel, and Residual A close-out

- [x] 4.1 **[D]** Route the main panels' parent-owned non-list wheel/chrome through `MouseGestureState`. ABS and Feeds — which have no `*HitRegion` enum — get their list scrolling and row hits resolved through `resolve_point` on the persistent canonical controls (Feeds' active control; ABS's narrow `InlineMediaBrowser`); the ABS wide rails are composed per-frame in the renderer and are not persistent controls, so their row hits resolve through the painted row rects (`book_rows`/`show_rows`) — same-geometry-as-painted, so no duplicate list-row coordinate path (scope-review correction). ABS book wheel = page-size move + re-request (Home/Queue precedent). Emby's already resolve through task 3.4.
- [x] 4.2 **[D]** Delete the remaining `if !self.focused { return None }` **mouse** guard — `feeds.rs:322`, in `FeedsComponent::handle_mouse` — so an unfocused visible panel responds to the pointer. (Verified against the tree before implementation: of the four guards D12 names, the other three — `audiobookshelf_book.rs:214` (`handle_key`), `audiobookshelf_podcast.rs:204` (`handle_key`), `home.rs:380` (`handle_crossterm_key`) — are **keyboard** guards, not mouse; `AudiobookshelfBookComponent::handle_mouse`, `AudiobookshelfPodcastComponent::handle_mouse`, and `HomeComponent::handle_mouse` already carry no focus check after the Phase 2 migrations, so there is nothing to delete there.) **This is an observable behaviour change** and is why it lands here rather than in Phase 1 (D12). Verify per surface, at both breakpoints, that a click/wheel on an unfocused panel acts and focus follows the click, and that the same event leaves other panels untouched.
- [x] 4.3 **[D]** Move the Home wheel to component-owned recognition (today `shell_home.rs:64` `handle_home_scroll`, which downcasts to the mounted `HomeComponent` and calls `move_local_cursor`): the throttle (`note_browse_scroll`) moves into the component's `MouseGestureState` — which already applies its 30 ms wheel throttle to the accepted `HomeScroll` — so `Model::handle_home_scroll` drops the `App`-side throttle read. Preserve the Continue Watching `cw_move_cursor` quirk and the context-menu target refresh (`push_home_content`). Then delete `handle_mouse_scroll_browse` (**verified no-op**: its `match` arms are all empty) at both of its live callers — the `BrowserScroll` feed-group arm (`shell_messages.rs:295`) and the `TvScroll` arm (`shell_messages.rs:474`; keep the adjacent `push_tv_workspace_content()`), — and, with all callers gone, `browse_mouse_ready` / `is_browse_layout_current` (`mouse_gestures.rs:12-23`) — ADR 0022 Residual A's first site. `note_browse_scroll` stays until task 6.3 (last consumer). Keep `normalize_stale_browse_destination` (callers at `cw_library_tab_actions.rs:43`, `library_load_actions.rs:80`, `shell_draw.rs:219`).
- [x] 4.4 **[M]** With all three sites closed (2.7, 2.8, 4.3), strike **Residual A** from `docs/adr/0022-…md` ("Residual debt" section) so "Known deviations" lists only Residuals B and C; verify no read of `layout.playback.seekbar_area`, `layout.main.browse_destination`, or `layout.tabs_area` remains outside the component that paints it (after 4.3 the only `browse_destination` reader is gone; the writer at `shell_draw.rs:178` is checked by the same grep for liveness).
- [x] 4.5 **[M]** Ensure parent click-to-focus and parent-owned controls are wired for all main panels; ABS list click/select/activate resolves through `resolve_point` (narrow) / painted row rects (wide rail) here (Emby's via 3.4) — not duplicated, reusing the existing move/intent Msgs (add `set_panel_focus(PanelFocus::Library)` to those shell arms where missing). Feeds select-only clicks have no Msg to ride on: add one `ShellRequest::FeedsRowClick` mirroring the `HomeRowClick` arm shape, whose shell arm applies `set_panel_focus(PanelFocus::Library)`. No other new Msg variants.
- [x] 4.6 **[D]** Wire parent-owned right-click/context-menu recognition and preserve the click-position anchor contract for every main panel that has a keyboard context-menu equivalent (Emby/Home/Queue/TV — already migrated in Phase 2, covered here as verification): the menu row identity comes from `resolve_point` and the anchor is a `(u16, u16)` the shell applies (`open_context_menu_at`). ABS book/podcast and Feeds have **no** keyboard context-menu action, so per the spec's parity clause ("right-click-to-menu where the surface has a corresponding keyboard action") their `RightClick` recognition arm ignores the event — no menu, no new anchor plumbing, no duplicate row-hit path.
- [x] 4.7 **[M]** Update the ledger Mouse columns for the five main-panel rows with owner, supported gestures, breakpoints verified (wide **and** narrow), and the test names.
- [x] 4.8 **[M]** Phase-3 gate: `rtk cargo nextest run -p mbv`, clippy, fmt; then live-review observable pointer/rendered-UI behaviour **at both breakpoints**. Treat defects as bugs and rerun affected tests and the gate before phase acceptance.

## 5. Phase 4 — Overlays, popups, and the already-handling surfaces

- [x] 5.1 **[D]** Add `Event::Mouse` handling (via the primitives) to `feeds_manage`, `library_routes`, `multiselect`, `save_playlist`, and `search_sidebar`: click-to-select and click-to-dismiss where a keyboard equivalent exists; verify per-component tests.
- [x] 5.2 **[D]** Bring the five surfaces that already handle mouse but appear in no migration task onto the primitives and the eligibility rule: `help.rs:159`, `sessions.rs:196`, `settings.rs:388`, `playlists.rs:302`, `inline_search.rs:249`. Each keeps its behaviour; what changes is that recognition comes from `MouseGestureState`, irregular regions from `HitRegions<Tag>`, and delivery from `mouse_sub()` + eligibility. Verify per-component tests, including that each is ineligible while a blocking overlay covers it.
- [x] 5.3 **[D]** Confirm the `confirm`, `daemon_lost`, and `remote_reanchor` blocking modals suppress all mouse activity beneath them **by eligibility** (D2): a `tick()`-level test asserts that a click outside the modal produces no underlying message **and** leaves underlying component state (cursor, scroll, selection) unchanged, since a message-discarding fold could not have guaranteed the second.
- [x] 5.4 **[D]** Verify `context_menu` and `selection_modal` mouse behaviour matches their specs (menu-click executes/closes, outside-click dismisses, wheel does not mutate the obscured view — the last now follows from exclusivity in D2 rung 2); run `rtk cargo nextest run -p mbv context_menu selection_modal`.
- [x] 5.5 **[M]** Update the ledger Mouse columns for every overlay/popup/modal row and the five surfaces from 5.2, recording breakpoints verified.
- [x] 5.6 **[M]** Phase-4 gate: `rtk cargo nextest run -p mbv`, clippy, fmt; then live-review observable overlay/popup pointer behaviour **at both breakpoints**. Treat defects as bugs and rerun affected tests and the gate before phase acceptance.

## 6. Phase 5 — Net-new mouse surfaces: Music narrow + narrow browse

_Both surfaces below are **new mouse work, not migration**. Music's narrow list
and track table have no `*HitRegion` enum and no narrow mouse path;
`src/app/components/browser/` (`content`, `keyboard`, `navigation`, `paint`,
`mod`) contains **no mouse handling at all** — there is nothing to keep and no
enum to delete. Budget them as design, not as a path swap._

- [ ] 6.1 **[D]** Design and build the Music narrow mouse surface: narrow list rows and the track table resolved through `resolve_point`, group pills and narrow/wide chrome in `HitRegions<Tag>`, gestures from the parent's `MouseGestureState` (wired in 3.6), click-to-focus, right-click anchor. Verify row and chrome gestures at the narrow breakpoint, and that the wide path from 3.6 is unchanged.
- [ ] 6.2 **[D]** Design and build narrow-browse mouse support from scratch in `src/app/components/browser/`: parent `MouseGestureState` recognition, list rows through `resolve_point`, non-list controls in `HitRegions<Tag>`, click-to-focus and right-click anchor, matching the wide Browser's gesture vocabulary. Verify at the narrow breakpoint against the wide behaviour as the reference.
- [ ] 6.3 **[M]** Now that the last consumers are migrated, delete the shell-side recognition glue: `note_browse_double_click`, `note_browse_scroll`, and `App.last_click_time` / `last_click_pos` / `last_scroll_at`. Verify `rtk cargo clippy --workspace --all-targets` reports no dead code and no caller remains.
- [ ] 6.4 **[M]** Update the ledger Mouse columns for the music-workspace and narrow-browse rows; confirm no row still reads `pending` and every multi-breakpoint row records both.
- [ ] 6.5 **[M]** Phase-5 gate: `rtk cargo nextest run -p mbv`, clippy, fmt; then live-review observable pointer/rendered-UI behaviour **at both breakpoints**. Treat defects as bugs and rerun affected tests and the gate before phase acceptance.

## 7. Phase 6 — Precedence proofs + close-out

- [ ] 7.1 **[D]** Land the "simultaneous Queue + Library" precedence proof: both visible, no overlay, a click on each resolves to the painting component through the real `tick()` synchronisation order, with focus following the click.
- [ ] 7.2 **[D]** Land the "blocking overlay swallows mouse" precedence proof through `tick()`: assert both that no underlying message is produced **and** that no underlying component was mutated — the guarantee eligibility provides and a message fold could not.
- [ ] 7.3 **[D]** Land the "geometry cannot drift" precedence proof: a component resolves a click from the same geometry it painted — for a canonical list, from the same `row_geometry` its painter consumed, exercised across a scroll and a breakpoint change.
- [ ] 7.4 **[M]** Verify the modified capability deltas match the implementation: no global hit map / global mouse router; no per-row rect registry duplicating a canonical control's row flow; the mouse-emitted `ShellRequest` variants have real handlers (no "mouse-only" no-op arms); `rtk ast-grep scan` clean.
- [ ] 7.5 **[M]** Final close-out: `rtk cargo nextest run -p mbv` full suite, `rtk cargo clippy --workspace --all-targets`, `rtk cargo fmt --check`, `rtk make check-code-file-lines`; confirm every ledger row has a filled Mouse column with breakpoints recorded, ADR 0022 no longer lists Residual A, and `openspec validate restore-mouse-support --strict` passes. Then complete live review and acceptance; treat any observable pointer/rendered-UI defect as a bug and rerun affected tests and gates before accepting the change.
