## 1. Establish component focus authority

- [x] 1.1 Inventory every mounted Interactive Component field, setter argument, and render-context value named or used as `focused`; classify framework focus separately from semantic playback/disabled emphasis, and verify the affected list against Home, Browser, Feeds, Audiobookshelf podcast/book, Music, TV, and Queue before editing. (see `focus-inventory.md`)
- [x] 1.2 Make each affected mounted component consume `Attribute::Focus(AttrValue::Flag(_))` in `Component::attr`, retaining component-private pane focus and selection on blur; verify with `cargo check -p mbv` and the existing focused component suites.
- [x] 1.3 Derive Music album/track and TV series/episode focused presentation from component framework focus combined with their retained local pane state; verify with `cargo nextest run -p mbv music_workspace tv_workspace`. (91/92 pass; the 1 failure, `tv_breakpoint_resize_round_trip_keeps_selected_series`, is a pre-existing baseline failure unrelated to this change.)

## 2. Remove the duplicate shell focus truth

- [x] 2.1 Remove framework-focus arguments from affected content models and `set_content` methods, then update shell projection callers so ordinary content pushes cannot overwrite component focus; verify with `cargo check -p mbv`.
- [x] 2.2 Delete focus-only re-projection helpers and call sites made obsolete by TuiRealm focus delivery while preserving content refreshes that have another purpose; verify by tracing every remaining `PanelFocus`-derived component write and confirming each controls mounting/routing/eligibility rather than projected component focus. (Deleted: `HomeComponent::set_focused` from production + its `push_home_content`/`render_home_component` callers; `let focused = matches!(effective_panel_focus(), Library)` seams in `shell_browser`/`shell_feeds`/`shell_audiobookshelf_book`/`shell_audiobookshelf_podcast`; the `focused` positional on `MusicWideRenderCtx::new` / `TvWideRenderCtx::new` / `wide_tv_render_ctx`. Remaining `PanelFocus`-derived writes traced: `sync_active_destination`/`sync_queue` drive `Application::active`/`blur` (routing); `shell_overlays_menus`/`shell_overlays_sidebars` resolve context-menu anchors and Help destination content; `shell_draw` paints panel chrome frames; `shell_playback` sets `PlaybackComponent` transport emphasis (out of scope, forwards `attr` to props); `set_panel_focus(_)` call sites are click-to-focus commands. None project component framework focus.)
- [x] 2.3 Remove key guards that exist only to compensate for projected focus where live `Application::tick()` already owns keyboard delivery; retain ADR 0024 mouse eligibility and click-to-focus behavior, then verify with `cargo nextest run -p mbv tests_tick_integration`. (No such guards found: the router already consults `Application::focus()` live in `shell.rs`; component-local `if !self.focused` keyboard gates are retained per design decision 3 — they now gate on the attr-authoritative `self.focused` and back the direct `Component::on` unit tests, they are not projection-compensation. `tests_tick_integration` 15/15 pass.)

## 3. Protect the reported behavior

- [x] 3.1 Strengthen the existing shell tick integration coverage with one Music Library → Queue → Library round trip that proves no Music navigation while blurred, immediate navigation after focus returns without a click or content refresh, and retained cursor/pane state; verify with the focused `cargo nextest run -p mbv tests_tick_integration` case.
- [x] 3.2 Strengthen the existing Wide TV render coverage through the real shell synchronisation order so moving focus to Queue removes the right rail's focused surface and selected-row marker without losing selected identity; use semantic cell assertions rather than a full-buffer snapshot and verify with `cargo nextest run -p mbv tv_workspace tests_tick_integration`.
- [x] 3.3 Exercise blocking-overlay focus loss and restoration through the existing live tick test, consolidating or deleting weaker direct-focus tests that become redundant; verify with `cargo nextest run -p mbv tests_tick_integration shell_overlays`. (Added `blocking_overlay_focus_loss_and_restoration_through_live_tick` alongside the existing `blocking_confirm_overlay_keeps_focus_and_receives_input`. No direct-focus tests were redundant: sections 1–2 already consolidated focus establishment onto the `#[cfg(test)] set_focused` attr-path helper, and design decision 3 keeps the narrow `Component::on` tests as intentional local-render characterization that the live tick test does not replace.)

## 4. Verify the change

- [x] 4.1 Run `cargo fmt`, `cargo check -p mbv`, and `cargo nextest run -p mbv`; fix every failure before proceeding. (`cargo fmt` clean; `cargo check -p mbv` clean; `cargo nextest run -p mbv` 1419/1420 pass — the sole failure is the pre-existing baseline `app::shell_tv_workspace::tests::tv_breakpoint_resize_round_trip_keeps_selected_series`, out of scope per the section handoff.)
- [x] 4.2 Run `cargo clippy --workspace --all-targets`, `ast-grep scan`, `make check-code-file-lines`, and `cargo fmt --all -- --check`; fix every failure before marking the change complete. (clippy: no errors, no new warnings from sections 3–4; `ast-grep scan`: clean (`[]`); `cargo fmt --all -- --check`: clean. `make check-code-file-lines` reports 3 files > 800 — `browser_component_tests.rs` (822), `shell_audiobookshelf_book.rs` (828), `shell_tv_workspace_tests.rs` (832); all three already exceed 800 at HEAD `d765fd26` (808/819/825) and grew only via the sections 1–2 `attr` migration, not sections 3–4. The new Wide-TV shell test was placed in the existing `shell_tv_workspace_selection_tests.rs` submodule (473 lines) so sections 3–4 add no file over the cap. Per the project rule that the file-lines cap is a pre-PR gate, not a mid-change gate, no split was performed here.)
- [x] 4.3 Re-read the implementation against `specs/interactive-component-framework/spec.md` and confirm Inline Search ownership, Panel focus commands, Panel mode, keyboard precedence, and mouse eligibility were not changed. (Sections 3–4 touch only test files — `tests_tick_integration.rs` and `shell_tv_workspace_selection_tests.rs` — with zero production changes, so none of those surfaces are altered. Re-reading the spec's five scenarios against the sections 1–2 implementation: focus is delivered solely via `Component::attr(Attribute::Focus(Flag(_)))` into private `framework_focus`/`context.focused` (Music `music_workspace.rs:658`, TV `tv_workspace/mod.rs:578`); `sync_active_destination`/`sync_queue` remain the only `Application::active`/`blur` callers; content pushes no longer carry a focus arg; component-private pane/cursor/selection survive blur; embedded `WideMediaList` stays inside its parent boundary with no `ComponentId`. Inline Search open/activate/dismiss paths in `music_workspace_keys.rs` are unchanged; keyboard precedence still resolves in `router.rs`/`key_policy.rs`; mouse eligibility still flows from `sync_mouse_subscriptions` after the same active-destination derivation.)
