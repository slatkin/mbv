# Tasks

## 1. Owner boundary

- [x] 1.1 Add a capability-gated, correlated idle queue-load command/result; carry the Queue source on the whole-queue replace/play command and reset the owner's source to Unknown on clear; add the source-only update carrying owner queue lineage, in `crates/mbv-core/src/ctrl.rs` and remote-player command handling, gating the owner side on `DaemonRole::Local` rather than `config.stay_alive`; verify round-trip/unsupported-peer tests reject without staging a private queue and a packaged-role owner does not take the new path.
- [x] 1.2 Implement owner stop/finalize-before-replace for accepted Stay-alive idle loads in `crates/mbv-core/src/daemon_control.rs` and Player lifecycle, reusing admission and permitting empty loads, and carry a Playback-run identity on stopped/completed observations (they carry none today) so a late report from the replaced run cannot apply progress or completion to a new-queue slot; verify mocked owner tests show one stopped snapshot, no playback start, rejection preserving the old run, and late old-run events ignored.
- [x] 1.3 Make the owner mint replacement lineage and apply guarded source-only updates without replacing slots or changing playback; verify tests for successful Save As propagation and rejection of a delayed update after another Client's replacement.
- [x] 1.4 Persist and reload the Stay-alive owner's own queue and source in its own state file: write on accepted queue-changing operations and on graceful shutdown, reload at startup, take over the existing per-user queue snapshot once when that file is absent, keep an empty queue empty with no older-snapshot fallback, and refuse Client adoption for the Local role; verify daemon tests round-trip a queue and source across restart, keep an empty queue empty, reject adoption, and leave a packaged-role owner on today's persistence.

## 2. Client authority

- [x] 2.1 Send non-autostart playlist loads to the home Stay-alive owner at load time from `src/app/queue_actions.rs`/`queue_scope.rs`, with no local replacement or Loaded claim until acceptance; verify focused Client tests for loading during playback, empty load, rejection, and disconnect. Bare mode, Direct remote control of another daemon, and packaged `mbvd` are unchanged; a Client attached to the Stay-alive process reads that owner's queue in every state, including while a Session or cast is the playback target.
- [x] 2.2 Reconcile Stay-alive queue slots, source and playback solely from owner snapshots in `src/app/player_event.rs` and queue projection, retiring generation-fence authority and the authoritative Client-side source copy for that owner (including the early `queue_source` assignment in `execute_pending_queue_action` and the local source set on play); verify two mocked Clients see the same stopped replacement, a later Play uses the owner slot, old queue indices never highlight the replacement, and a play or clear leaves the displayed source equal to the owner's.
- [x] 2.3 Route successful Save As source changes through the guarded owner source update and keep playlist mutation lineage checks, without resubmitting contents; verify an in-flight Save As cannot rename another Client's newly loaded queue.
- [ ] 2.4 Remove Client-side seeding and persistence of the Stay-alive queue: delete the `bootstrap_local_daemon_queue` adoption branch and its `UnifiedAdoptQueue` use (`src/app/bootstrap.rs`, `construct.rs`, `daemon_restart.rs`), and stop `save_queue_state`/`restore_queue_state` for the Stay-alive owner; verify attaching to an empty owner shows empty and cannot resurrect a saved snapshot, while Bare-mode restore is unchanged.

## 3. Continuity and verification

- [ ] 3.1 Verify owner persistence continuity end to end: restart reloads the owner's queue and source, an empty queue stays empty across restart and attach, a Client with a stale saved snapshot cannot seed the owner, and Bare-mode persistence still restores.
- [ ] 3.2 Review queue/snapshot owner invariants and update `docs/invariants/`, sync the two delta specs into `openspec/specs/` (including the narrowed `Canonical-queue edits follow the playback target` requirement), record the Stay-alive/mbvd seam in a short ADR beside 0015 (the Stay-alive process owns and persists its queue and source; packaged `mbvd` keeps its current behavior; the owner-side gate is `DaemonRole::Local`), and update `CONTEXT.md` if new domain terms were introduced (owner-held queue and source); verify strict OpenSpec validation and no stale conflicting requirement remains.
- [ ] 3.3 Verify the coherent flow with `cargo check -p mbv-core`, `cargo check -p mbv`, focused `cargo nextest run -p mbv-core` and `cargo nextest run -p mbv`, `cargo fmt --all -- --check`, and `cargo clippy --workspace --all-targets -- -D warnings`; manually check two attached TUIs loading while one video plays and a daemon restart holding a non-empty queue, recording the observed results before archiving.
