# Tasks

## 1. Shared TreeBrowser data and owner

- [x] 1.1 Add `src/app/components/list/tree_browser/` with destination-neutral `TreeNode<Target>`, optional trailing metadata, closed mark policy, semantic operations, transitions, and `TreeBrowser<Target>` ownership. Require `TreeBrowser<Target>: tuirealm::Component`; keep it embedded, never independently mounted/focused/subscribed or assigned a `ComponentId`, and expose only stable-target surfaces from `list`. Verify: `cargo check -p mbv` is clean, the generic type implements `Component`, and no public type exposes `tui_treelistview`.
- [x] 1.2 Move the monotonic arena, target interning, roots/children, revision tracking, and atomic reconciliation into `TreeBrowser<Target>` so arbitrary-depth plain node projections preserve selection and expansion by stable identity. Accept only an acyclic forest; duplicate targets, missing parents, self-parenting, and cycles return a typed reconciliation error without changing content, selection, expansion, marks, viewport, or retained geometry. Verify: focused non-Music tests cover reorder, removal fallback, three levels, every rejection class, typed errors, and unchanged state after rejection.
- [x] 1.3 Add the non-Music three-level fixture required by design D7 and exercise the complete public component through `Component::view` with a distinct target enum. Verify: it covers movement, paging, first/last and parent/child traversal; persistent versus filter-forced expansion; ordered marks, aggregation, exclusions, and display-order intents; atomic reconciliation and stable-identity preservation; one operation returning all independent transition facts; completed-frame point resolution and invalidation; and indentation, depth roles, zebra, selected/aggregate bars, metadata gutter, marquee, and scrollbar without importing any Music type.

## 2. Shared operations and state

- [x] 2.1 Move movement, first/last, visible-viewport paging, parent/child traversal, expansion/collapse, geometry clamping, and selection fallback behind the closed `apply(TreeOperation<Target>) -> TreeTransition<Target>` surface using the existing `Cursored`, `Viewported`, and `Expandable` mechanics. Except for construction, atomic reconciliation, Panel configuration, and read-only stable-target queries, expose no destination-visible mutation path. Verify: focused shared-tree tests cover clamp-no-wrap, minimum keep-visible movement, branch collapse, parent/child traversal, responsive viewport continuity, and absence of alternate movement/expansion mutators.
- [x] 2.2 Move the filter session, fuzzy matching over supplied search text, filter anchor, forced expansion, dismissal restoration, and hidden-mark retention into `TreeBrowser`. Verify: focused tests cover matching at each hierarchy level, no-match state, query replacement, anchor restoration, and filter-forced expansion not becoming persistent expansion.
- [x] 2.3 Move ordered marks, mark-policy enforcement, descendant aggregation, display-order action targets, and pointer-resolved mark operations into `TreeBrowser`, returning one transition per operation. Verify: focused tests cover aggregate/partial state, excluded nodes, hidden descendants, addition order versus action order, and latest-frame stable-target pointer resolution.
- [x] 2.4 Replace direct tree-mutator sequencing with the sole shared `apply` surface for activation, context, pointer selection, filter editing, marking, movement, and expansion. Transitions independently report consumption, selected-target change, mark-summary change, and external intent. Verify: focused tests demonstrate one operation producing multiple facts, and API review confirms no destination-visible mutation bypass for movement, expansion, filter, mark, pointer, activation, or context behavior.

## 3. Shared tree presentation and Panel integration

- [x] 3.1 Add one destination-neutral TreeBrowser Render Component under `src/app/render/components/tree_browser/` and move the current label renderer and fixed visual policy into it: plain indentation, depth roles, semantic playback emphasis, selected and aggregate-mark bars, group-relative zebra, focused marquee, and optional trailing metadata. Destinations pass semantic data only and never call the Render Component directly. Verify: focused Render Component tests preserve conditional metadata-gutter width, no state glyphs, and the existing Music pixels.
- [x] 3.2 Make `TreeBrowser::Component::view` the only interactive view entry point and delegate tree widget composition, parent insets, full-width selected claims, temporary crate-buffer handling, shared scrollbar, and completed-frame geometry retention to the shared Render Component. Provide no inherent or destination-owned alternative view method. Verify: existing Wide and smallest non-Wide buffer tests retain their assertions, and pointer tests prove changed or incomplete geometry claims no point.
- [x] 3.3 Replace `impl PanelList for MusicTreeBrowser` with one generic `impl<Target> PanelList for TreeBrowser<Target>` that receives only focus and geometry from the Library panel and invokes `Component::view`. Verify: Library-panel tests paint and hit-test the shared tree through `ListSlot::Media` with no tree-specific slot arm or alternate view entry point.

## 4. Convert Grouped Music into a consumer

- [x] 4.1 Keep `MusicTreeTarget` as Music's stable identity and replace `MusicTreeEntry`/`MusicTreeTrack` model construction with one projection from Artist, Album, and cached Track snapshots into `TreeNode<MusicTreeTarget>`. Verify: existing reconciliation tests preserve settled order, duplicate display-name identities, selection, expansion, and cached Track children.
- [x] 4.2 Translate shared selection and navigation transitions into the existing Music persistence, workspace, artwork-prefetch, and artist-track requests without reading component internals or recomputing a target from a position. Verify: focused Music owner tests preserve album persistence, Artist focus behavior, Workspace resolution, and neighbour artwork prefetch.
- [x] 4.3 Translate shared activation and context intents into the existing Artist, Album, and Track shell requests, including ordered multi-selection and filtered Artist scope. Verify: existing Music keyboard, mouse, context-menu, and real-`Application::tick()` integration tests remain green with stable-target requests.
- [x] 4.4 Route Grouped Music filter input and search-bar projection through the shared filter session, deleting destination-owned tree filter state and direct query mutation. Verify: existing Music filter keyboard, pointer, restoration, and responsive tests remain green.
- [x] 4.5 Preserve Grouped Music Hero and Workspace composition while replacing its browser field with `TreeBrowser<MusicTreeTarget>`; make no TV, Hero, Workspace, shell-effect, playback, or router behavior change. Verify: focused Music Hero/Workspace and Wide/non-Wide composition tests pass without altered appearance assertions.

## 5. Delete the destination-specific implementation

- [ ] 5.1 Delete `music_tree.rs`, `music_tree_model.rs`, `music_tree_selection.rs`, `music_tree_view.rs`, and `music_tree_label.rs`; relocate only `MusicTreeTarget` and plain projection/target-resolution helpers. Require `MusicContent` to contain exactly one tree-control field, of concrete type `TreeBrowser<MusicTreeTarget>`. Music production modules contain no tree model/query/state, expansion/filter/viewport/mark/geometry/marquee carrier, tree painter, renderer, or forwarding browser API under any name. Verify: structural source review establishes each condition rather than checking only legacy identifiers.
- [ ] 5.2 Confine every production and test reference to `tui_treelistview` to `src/app/components/list/tree_browser/` and `src/app/render/components/tree_browser/`; confirm both shared modules import no Music domain type or module. Verify: no module outside those boundaries imports, names, constructs, or asserts against a `tui_treelistview` type, while outside tests use only stable targets, shared transitions, retained geometry, and rendered output.
- [ ] 5.3 Delete private-arena tests tied to the removed Music implementation and retain or relocate only tests that protect shared component behavior or observable Music behavior; do not duplicate assertions already owned by stronger buffer or tick tests. Verify: focused shared-tree, Music component, Music render, and Music tick-integration test groups are green.

## 6. Verification and close-out

- [ ] 6.1 Run `cargo nextest run -p mbv` with the shared-tree, Music component, Music render, and mounted Music interaction coverage in place. Verify: the package suite is green with no TV test or production edit required by this change.
- [ ] 6.2 Run `cargo clippy --workspace --all-targets -- -D warnings`, `cargo fmt --all -- --check`, and `cargo nextest run --workspace`. Verify: all commands are clean.
- [ ] 6.3 Sync the `shared-list-components` delta into `openspec/specs/`, add the `TreeBrowser` term to `CONTEXT.md` without renaming an existing term, and run `openspec validate --all`. Verify: the main spec states the complete-component contract, the term names its ownership boundary, and validation is clean.
- [ ] 6.4 Commit the implementation and associated planning/spec/domain artifacts, then archive `extract-shared-tree-browser`. Verify: `openspec list --json` no longer reports the change active, `openspec validate --all` remains clean, and `git status --short` is empty.
