# Handoff: Task 8.1 Test Compilation

## Status

`cargo check -p mbv` passes clean (production code compiles, only pre-existing warnings).

`cargo nextest run -p mbv --no-run` fails with ~28 test compilation errors due to 8.1 deleting `App::handle_key`, `App::handle_key_with_home_context`, `GlobalViewKey`, `CONTEXT_STACK`, `typed_key`, `to_crossterm_key_event`, and ~9 `CONTEXT_STACK` handler functions.

The full task 8.1 agent (`RemoveLegacy81b`) ran 31 min and did the heavy lifting on production code. This handoff covers the unfinished test-side cleanup that 8.1 implies.

## What Changed in 8.1 (verified passing `cargo check`)

### Production fixes applied after the 8.1 agent ran

1. **`src/app/components/browser.rs`** — restored mangled `Component::on` AR. Agent's edit left `Event::Keyboard(key) => self.handle_tui_key(*key)` as an orphaned line at the top of `impl AppComponent`; needed to wrap it back into a proper `fn on(&mut self, event: &Event<UserEvent>) -> Option<Msg>` body. Also fixed the ALT guard that still matched `crossterm::event::KeyCode::Left` after the agent switched to `tuirealm::Key` types — changed to `Key::Left | Key::Right | Key::Up | Key::Down`.

2. **`src/app/components/settings.rs`** — missing `}` after `Key::PageUp =>` arm (300-303). The agent deleted the closing brace of the match arm but left `Key::Left | Key::Right | ...` directly below, which became an unclosed-delimited child block.

3. **`src/app/components/root.rs`** — added `TerminalObserverEvent` to the `use super::*` line. The agent left the references in place without importing the type.

4. **`src/app/input_resolver.rs`** — the agent stripped this file to only `KeyChord`, but `key_policy.rs` still uses `InputContext`, `InputSnapshot`, `KeyResolution`, and `resolve_key`. Re-added all four:
   - `enum InputContext { Playback }`
   - `struct InputSnapshot { player_active: bool, has_remote_session: bool }`
   - `enum KeyResolution { Command(Command), Swallow, FallThrough }` (no `Eq` derive — `Command` doesn't impl `Eq`)
   - `fn resolve_key(context, snapshot, chord) -> KeyResolution` — thin wrapper over `playback_command_for_key`
   
   Also added `pub(super) fn tuirealm_key_to_crossterm(key: tuirealm::event::KeyEvent) -> crossterm::event::KeyEvent` — needed because `shell.rs` `router_outcome` extracts a `tuirealm::event::KeyEvent` from `TerminalObserverEvent::Key` but `resolve_router_outcome` and `update_double_tap_state` take crossterm keys. Wildcard arm handles `CapsLock/ScrollLock/NumLock/PrintScreen/Pause/MediaKey` etc.

5. **`src/app/key_policy.rs`** — first re-added the deleted imports (`resolve_key`, `InputContext`, `InputSnapshot`, `KeyResolution`), then removed the over-eager `playback_command_for_key` direct call I added in `KeyPolicyGate::Playback` so it goes through `resolve_key` again (matches original logic).

6. **`src/app/shell.rs`** — added `let key = super::input_resolver::tuirealm_key_to_crossterm(tui_key);` in `router_outcome` after extracting the key from messages. The router and double-tap timer still take crossterm types.

7. **`src/app/shell_playback.rs`** — `ToggleVisualizer` arm was calling `self.app.handle_key(...)` (deleted). Changed to `let _ = self.app.dispatch(super::action::Command::ToggleVisualizer);`. Also removed the unused `use crossterm::event::{KeyCode, KeyEvent, KeyModifiers};` import.

8. **`src/app/input.rs`** — removed unused `SidebarId` and `crossterm::event::{KeyCode, KeyEvent, KeyModifiers}` imports. The 8.1 deletion of `handle_key*` methods made them dead.

9. **`src/app/input_confirm_keys.rs`** — the agent's version had stale `ConfirmAction::Quit`, `DeletePlaylist(idx)`, `RenamePlaylist`, `OverwritePlaylist`, `RemoveFeedSource`, `UndoQueueChange` arms referencing methods that never existed or were already deleted. Restored the original dispatch function matching the current `types_confirm.rs` variants exactly:
   ```rust
   match action {
       ConfirmAction::ClearQueue => ...
       ConfirmAction::RemoveActiveQueueItem(pos) => ...
       ConfirmAction::RescanLibrary(lib_idx) => ...
       ConfirmAction::SaveOverwritePlaylist { existing_id, name } => ...
       ConfirmAction::DeletePlaylist { id, name } => ...
       ConfirmAction::RemoveFeedSubscription(index) => ...
       ConfirmAction::RemoveEmby => ...
       ConfirmAction::ReplaceEmby(generation) => ...
       ConfirmAction::RemoveAudiobookshelf => ...
       ConfirmAction::ReplaceAudiobookshelf(generation) => ...
       ConfirmAction::DiscardOrSaveDirtyPlaylist => ...
   }
   ```
   Methods used (all exist on `App`): `replace_queue_or_prompt`, `remove_active_slot_confirmed`, `clamp_cursor`, `pending_delete_slot`, `playback_target`, `player.stop`, `local_queue_metadata_applies`, `queue_dirty`, `retire_remote_tracking`, `trigger_lib_rescan`, `do_overwrite_playlist`, `open_save_playlist_dialog`, `spawn_delete_playlist`, `remove_feed_confirmed`, `remove_emby_confirmed`, `pending_emby_replacement`, `replace_emby_confirmed`, `remove_audiobookshelf_confirmed`, `pending_audiobookshelf_replacement`, `replace_audiobookshelf_confirmed`, `save_playlist_to_emby`, `pending_queue_action`, `execute_pending_queue_action`, `request_sidebar_dismiss`, `set_panel_focus`.
   
   Also restored `pub(super) fn request_clear_queue(&mut self)` (used by `shell_queue.rs:128` for `QueueIntent::Clear`) — guards on `PanelFocus::Queue && QueueScope::Remote`, shows `ToastSeverity::Error`, asks confirm modal with `ConfirmAction::ClearQueue`.
   
   Also restored `pub(super) fn handle_key_clear_queue_prompt` (legacy 'c' handler that asks the clear-queue confirm; not called from production anymore but kept for parity with original).

## What's Still Broken: Tests Don't Compile

`cargo nextest run -p mbv --no-run` fails. The 8.1 agent deleted test files: `input_movie_detail_tests.rs`, `input_music_track_navigation_tests.rs`, `input_library_scope_routing_tests.rs`, `input_resolver_tests.rs`, `input_resolver_handle_key_tests.rs`, `tests_audiobook.rs`, `tests_services_settings.rs`. The remaining test files still reference deleted infrastructure.

### Error categories (from the ~28 errors)

1. **Stale `App::handle_key` calls in tests** — likely 10-15 sites. Replace with the typed-message equivalent (most are `drive_key` / `dispatch` helpers) or delete the test if it covered only the deleted bridge.

2. **Stale imports of deleted types** — `ShellRequest::GlobalViewKey`, `KeyCode`, `KeyEvent` from `crossterm::event` in test modules where those are no longer needed.

3. **`//! expected item after doc comment`** — one test file has a stray doc-comment block at the top level.

4. **`!` on type `()`** — likely a `!matches!(...)` where the matched expression now returns `()`.

### Files most likely affected

Run `rtk cargo nextest run -p mbv --no-run 2>&1 | grep -B2 'error' | head -120` to get the exact list. Suspects from a prior grep:
- `src/app/input_resolver_handle_key_tests.rs` (deleted by agent — but still declared in `mod.rs`?)
- `src/app/tests_audiobook.rs` (deleted by agent)
- `src/app/tests_services_settings.rs` (deleted by agent)
- `src/app/input_music_track_navigation_tests.rs` (deleted by agent)
- Any test file under `src/app/` that imports `ShellRequest`, calls `App::handle_key`, or uses `crossterm::KeyCode` directly

## Suggested Approach for the Test Fixup

The 8.1 cleanup is already a large change. Test fixup is best split across multiple subagents by file. **Do NOT delegate the whole thing to one agent** — it will time out again.

### Plan

1. **First**: list the actual errors with `rtk cargo nextest run -p mbv --no-run 2>&1 | grep -E '^error|^   --> '` and group by file.

2. **Then** fan out one subagent per file (or per 2-3 files if they're small). Each subagent's brief:
   - Target: exact files and tests listed in their prompt
   - Change: delete dead tests OR rewrite to call the new typed-message equivalents
   - Acceptance: `rtk cargo nextest run -p mbv --no-run 2>&1 | grep error_count_for_your_files` shows zero errors from those files
   - Constraint: do NOT change production code; only test files

3. **Verification**: parent runs `rtk cargo nextest run -p mbv --no-run` once after all subagents complete. No need to run tests themselves — only compile.

4. **Then**: 8.1 test fixup → 8.2 (architecture gates, leftover tests) → 9.1 (full fmt/check/test/clippy) → 9.2 (architecture/file-size/production searches).

## Open Decisions (NOT made yet)

- Some tests that the 8.1 agent deleted were probably *covering* the deleted bridge behavior. Replacement tests against the central router should be added in task 8.2 (architecture gates) — that is when the production-side tests for the router surface land, not now.
- `request_clear_queue` is now in `input_confirm_keys.rs` but is only called from `shell_queue.rs`. If `QueueIntent::Clear` is the only entry, the legacy `handle_key_clear_queue_prompt` could be deleted too — but skipping that as opportunistic.

## Files Touched in 8.1 (verified)

Production (clean `cargo check`):
- `src/app/components/browser.rs` — Component::on restored; ALT guard fix
- `src/app/components/settings.rs` — fixed brace
- `src/app/components/root.rs` — TerminalObserverEvent import
- `src/app/input_resolver.rs` — restored types + tuirealm→crossterm converter
- `src/app/key_policy.rs` — restored imports
- `src/app/shell.rs` — TuiKey → crossterm conversion in router_outcome
- `src/app/shell_playback.rs` — ToggleVisualizer via dispatch
- `src/app/input.rs` — removed dead imports
- `src/app/input_confirm_keys.rs` — rewrote dispatch, restored request_clear_queue

Deleted by 8.1 agent (verified safe):
- `src/app/typed_key.rs` (and `mod typed_key;`)
- `src/app/input_feed_tab_keys.rs`
- `src/app/input_movie_detail_tests.rs`
- `src/app/input_music_track_navigation_tests.rs`
- `src/app/input_library_scope_routing_tests.rs`
- `src/app/input_resolver_tests.rs`
- `src/app/input_resolver_handle_key_tests.rs`
- `src/app/tests_audiobook.rs`
- `src/app/tests_services_settings.rs`

Test files NOT deleted but now broken (need fixup):
- TBD — run the error grep to find

## Risks

- `tests_routing_matrix.rs` may need updating: it imports `RouterSnapshot`, `resolve_router_outcome`, `apply_router_outcome`. Those still exist. Likely fine, but verify.
- `shell_home.rs` had a reference to `Msg` import that's now unused. Same for `shell_inline_search.rs`, `shell_root.rs`, `shell_overlays.rs`, `shell_overlays_tests.rs`, `tests.rs`, `render/mod.rs`, `render/components/library_routes.rs`. These are warnings, not errors — leaving them is fine for now; task 9.1 cleanup will handle.

## Notes for Next Agent

- Subagents MUST `cd /home/slatkin/Dev/mbv/.worktrees/migrate-tui-to-tuirealm` — every `rtk` command.
- Skip formatters, clippy, and full test runs — only `cargo nextest run --no-run` is needed to verify compilation.
- `cargo check -p mbv` already passes; do not touch production code unless an error trace points to it (it shouldn't — every current error is in `#[cfg(test)]` code or test modules).
- If a test file is beyond saving (50+ errors, references half the deleted infrastructure), delete it cleanly and document in the handoff which file was removed and why.
