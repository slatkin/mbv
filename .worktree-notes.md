# Next Steps — unify-playback-queue-model

## Status (as of 2026-08-09)

### Tasks 1–4: COMPLETE ✅
All implementation tasks are done. The canonical `PlaybackQueue<QueueItem>` is the single source of truth across the daemon, player, ctrl protocol, and TUI.

### Task 5: Verification — COMPLETE ✅
All three verification commands pass:

| Command | Result |
|---------|--------|
| `rtk cargo check -p mbv-core` | ✅ clean |
| `rtk cargo check` | ✅ clean |
| `rtk cargo test -p mbv-core` | ✅ 385 passed |
| `rtk cargo clippy --workspace --all-targets` | ✅ 0 errors (1 pre-existing warning: large size difference in `player_types.rs:155`) |
| `rtk make check-code-file-lines` | ✅ all files ≤ 800 lines |

### Task 5.1: Delete legacy artifacts — PARTIAL
The following have already been removed:
- `play_feed` / `PlayFeed` — gone
- Feed-only headless, fake-client, reporter, run-construction policy — gone
- Parallel `feed_items` from `PlayerTab` — gone (replaced by unified `PlaybackQueue<QueueItem>`)
- GUID-wide consumption helpers — gone

The following are **retained as documented wire-compat adapters** (per task 5.1):
- `WireCommand::LoadFeed` — intercepted at daemon control boundary, converted to unified queue path
- `CtrlState.feed_items` — still part of the legacy wire format for old peers; populated from `split_queue_for_legacy()`
- `feed_items: Vec::new()` in test fixtures — harmless defaults for legacy struct fields

### Task 5.2: Specification matrix
Needs manual review against the spec. The spec lives at:
- `openspec/changes/unify-playback-queue-model/specs/` (if present)
- `openspec/changes/unify-playback-queue-model/design.md` (if present)

## Remaining Work

1. **Review task 5.2 spec matrix** — verify Emby and Feed items across:
   - bare local playback
   - cold stay-alive playback
   - warm playback
   - direct remote control
   - reconnect
   - mixed reordering/removal
   - duplicate slots
   - persistence
   - failure visibility
   - audio-only admission/fall-through

2. **Address clippy warning** (optional, pre-existing): `player_types.rs:155` — large size difference between variants. Could be boxed if desired.

3. **Open PR** — once 5.2 is verified, push the branch and create a PR.

## Spec Link
- Tasks: `openspec/changes/unify-playback-queue-model/tasks.md`
- Worktree: `.worktrees/unify-playback-queue-model`
- Branch: `refactor/unify-playback-queue-model`
